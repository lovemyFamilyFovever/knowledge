# handoff-2026092503 · 彻查轮次 24：§2 九个「未测」清零 + 修掉一个潜伏多年的失效

> 接手第一步：`git log --oneline -3` + `git status`。前两份：`docs/handoff-2026092501.md`（彻查链闭合）、`docs/handoff-2026092502.md`（轮次 23 三件拍板）。

## 项目速览

个人知识库单一入口（`E:\GitHub\knowledge`）：`content/` 的 md/html 是唯一事实源，Flask 本地阅读器 + 本地语义检索。契约在 `AGENTS.md`，审查账本在 `测试文件/覆盖台账.md`。

## ① 我们在做什么任务

轮次 23 收口后，我列过"还剩什么可做"。用户挑了其中一条：**"跑下 §2 剩的 9 个未测功能"** —— 把台账 §2 那 9 行"控件在位、但点了有没有反应从来没人断言"的格子做成真断言。

## ② 已经完成了什么

**新建第 13 套 smoke：`tests/test_ui_behavior.py`（78 断言，约 90s，缺 node/Chrome 自动 SKIP）**，已进 pre-commit（按条件触发：本次提交含 `static/*.js`·`static/*.css`·`app/templates/*.html`·`app/routes_files.py`·`app/routes_pages.py` 时才跑）与 CI。

| 探针 | 断的是什么"反应" |
|---|---|
| 问吧 `showAsk()` | 浮层开出 → 空态 4 张卡 → 发送后 pending 出现 → 空态撤下 → 503 降级给出 `KB_AI_API_KEY` 提示（不是空白）→ pending 被清、按钮复可用 |
| `[[` 双链补全 | **CM 接管分支与 textarea 回落分支各自验**：浮层弹出、候选是真标题、采纳后 `[[名字]]` 插回缓冲、浮层收起 |
| 标签补全浮层 | 懒绑定真的绑上 → 前缀过滤出候选 → 回车是"填充"而不是"提交" → 采纳后收起 |
| mermaid 放大 | 注入的片段过 `node --check` → `dataset.kbZoom` → 点击加 `show` 并克隆 SVG → 点遮罩关闭；**对照组**：不含 mermaid 的 HTML 不许被注入 |
| 空态入口 `#kb-empty-newdoc` | 弹窗命名 → **落盘**（frontmatter + 正文都在）→ 前端跳进新文档 |
| crumb 删除 | 弹确认 → crumb 变「已删除」→ 撤销入口 → 文件进 `_trash/`（软删，不变量 4） |
| 编辑器 `#ed-del` | 删除后编辑器强制收起 + **备注旁挂 `.notes.md` 跟着正文一起进 `_trash/`** |
| 收件箱 del / purge | 丢弃=软删；purge **要确认词**：写错时一个字节都不删（护栏），写对时正文+sidecar+同名美化版三件物理删且不留 `_trash` 副本 |
| 标签页看板抽屉 | 点胶囊展开 → 标题带标签名与篇数 → 文档行有标题与链接 → 再点收起 → 第三次能再展开（状态可逆） |

**顺带清掉 §5 一格**：`routes_pages._rewrite_html_assets` 原记"模块私有、要测得先导出"——不用导出，它是 `/raw/<*.html>` 的必经之路，从 HTTP 面打进去就是同一份代码（CDN→vendored 两条映射、`./_shared/` 归一、`mermaid-code`→`mermaid` 容器名归一、归一后浮层照样注入）。

**第一版就抓到一个今天就坏、潜伏多年的产品缺陷（§6 第 32 行）**：美化版 HTML 的 mermaid「点击放大」**从来没生效过**。`_ZOOM_SNIPPET` 是用普通 Python 字符串写的 JS，里面的 `"\n"` 被 Python 先解析成**真换行** → 注入页面的脚本成了未闭合字符串 → 整段 SyntaxError → 容器、绑定、缩放一行都没执行。图本身照常渲染（mermaid.js 是另一段），所以肉眼看不出，只有"点图没反应"。修法是把常量改成 `r"""`（一行），回归锁是 `node --check` + 三条浏览器断言。

**六处定点变异全部验证锁会咬**（控制组先绿再谈归因）：摘掉 `r` → 3 条红；`has_mm=True` 无条件注入 → 对照组红；purge 确认词校验短路 → 3 条红（含"一个字节都没删"）；删除时不带 sidecar → 2 条红；抽屉不 `hidden=false` → 2 条红；问吧不撤空态 → 1 条红。

台账：§0 轮次 24（199 / **148** / **51**，对账器已验）、§2 九行 `未测`→`E2E`（§2 已无 `未测`）、§5 一行 `未测`→`已覆盖`、§6 第 32 行、§7 新增门禁行 + 套数改 13、§8/§14.1/§14.4 刷新。

## ③ 当前卡在哪

没卡。工作区里 `docs/archify/landing-chrome-overrides.css`、`static/archify/zhiku-pipeline.html` 是别的会话的，**不要碰、不要提交**。

## ④ 下一步计划

自动动作为空。可选项（都要用户点头）：

1. §2 的 49 行 `部分` —— 有交互、有截图，但没有"点击后状态"的判语。本套已经证明这类断言写得出来（9 行一次清完），但再往下 40 多行的成本是每加一行约 10s 门禁时间，且多数控件（拖拽、右键菜单具体条目、滑杆拖动）的 oracle 得先设计。
2. §5 的 `HFTokenizer.tokenize` 逐 token 对齐：要装 `tokenizers`（新依赖）。
3. `/stats` 图表本体无基线：要先造确定性 reading.db 事件。
4. 明确不做：P7 真实书库 fuzz（用户否决）、bandit/semgrep（新依赖）。

## ⑤ 踩过的坑（绝对不要再踩）

1. **无头浏览器里 `.focus()` 不一定送 focus 事件**。这台机器上 `document.hasFocus()` 是 `false`，`inp.focus()` 只把 `activeElement` 挪过去，`addEventListener('focus')` 收不到 —— 而标签补全是**懒绑定**（首次 focus 才 `TagSuggest.bind`）。表现就是"浮层永远弹不出来"，看着像产品 bug，其实是测试环境。判据：给 `TagSuggest.bind` 打桩数调用次数（`focus()` 后 0 次、补一次 `dispatchEvent(new FocusEvent('focus'))` 后 1 次）。**真人用鼠标点进去一定会发 focus**，所以这是补环境的缺口，不是绕过缺陷。
2. **`evalcdp.mjs` 的视口只有 ~764px 宽**，右栏在那一档没有布局：元素 rect 全是 0、`focus()` 无效。凡是依赖侧栏/浮层定位的行为断言，都用 `scripts/agent/geom.mjs <url> 1600 @expr` 钉一个桌面宽度跑（geom 已支持 `awaitPromise`，能跑 async 表达式）。
3. **要断"中间态"的探针必须分两趟跑，中间由 Python 查磁盘**。收件箱 purge 的"确认词写错 → 不该删"合成在一趟里时，第二次（正确词）尝试紧接着把文件删了，Python 检查永远看到最终态 → 那条断言恒假（第一版就这么假红过）。
4. **破坏性探针一律排最后**。第一版把删除排在看板抽屉之前，等抽屉去 `/tags` 时承载「渲染」标签的两篇文档已经进 `_trash/`，标签云里没那个胶囊 → 6 条集体假红。
5. **Python 字符串里写 JS/CSS 常量必须 raw**。`\n` 会被 Python 先吃掉，落到页面就是未闭合字符串 —— 这就是 §6 第 32 行那个"从来没生效过"的成因，而且它能潜伏多年全靠"没人点过那个图"。
6. **对照组不能省**：注入类逻辑要同时断"该注入的注入了"和"不该注入的没注入"，否则"无条件注入"也算绿（`has_mm = True` 变异就是专门用来验这条的）。
7. **别用 `grep` 读中文测试输出**：控制台是 GBK，中文断言名会花屏，`grep "状态栏"` 找不到 ≠ 没失败。落文件后用 `io.open(..., encoding='gbk', errors='replace')` 读，或只看退出码。
8. **一次只跑一个浏览器套**：P5 / js_props / ui_behavior 都要起 headless Chrome + 临时实例，并发会互相顶红（本轮又踩到一次输出交错）。
9. `content/小说/` 不读、不写脚本、不再提议（用户定的边界，AGENTS 禁区）。本套所有语料都是 `tempfile` 现造的合成样本。

## 给新会话的第一句话

"接手 `E:\GitHub\knowledge`：先读 `AGENTS.md` 与 `docs/handoff-2026092503.md`，再看 `测试文件/覆盖台账.md` §0 最新一轮。彻查链与 §2/§5 的『未测』都已清零，工作区里 `docs/archify/`、`static/archify/` 的改动不是你的，别提交。"

---

## 补记 · 轮次 25（同一会话继续，用户说"继续"）

第 13 套加了"探针 0"，把 §2 顶栏那 9 行 `部分` 也升成 `E2E`：现在断的是**点了真的换页**
（出发页 `/` 高亮在「阅读」→ 点「复习」→ 到新文档上下文里读到 `pathname=/review`、标题变了、
高亮搬过去、`#kb-learn` 渲染出来），外加九个目的地逐条 HTTP 面断"特征元素 / 高亮归属 / 标题前缀"。
为此给 `scripts/agent/geom.mjs` 加了 `KB_GEOM_CLICK`（先点再求值；点击引发整页导航时，
求值发生在**导航之后**的新文档里 —— 这才是"换没换页"的直接证据）。套件现在 **114 断言 / 96s**。

**又抓到一条口径不一致（§6 第 33 行）**：收件箱把备注旁挂 `.notes.md` 当成待归档条目 ——
顶栏徽标与列表都会多出一条"某某 的备注"，而全仓对 `.notes.md` 的口径是不可见
（`scan_corpus` 的 B2），且 `/api/inbox/purge` 本来就把它当附属品跟着正文一起删。
归档它的后果是把一条无处渲染的备注塞进正式域目录。已在 `inbox_iter` 补豁免，
并把"badge == 列表行数 == 磁盘真篇数"三处一起断（定点变异：摘掉豁免 → 2 条红）。

两条如实登记（都不是断言写松）：`/stats` 在无阅读数据的实例里只渲染骨架，KPI/图表/复习覆盖区
全有条件，所以特征元素取那张永远在的 `#st-daily-data`；从图标进 `/inbox` 与 `/home` 时
一级导航**没有任何一项高亮** —— 现状如此，改了这个行为这条会红。

台账：§0 轮次 25（199/**157**/**42**，对账器已验）、§2 九行升 `E2E`、§6 第 33 行、§14 数字同步。

### 再补 · 轮次 26（阅读排版偏好 7 行）

第 13 套加"探针 10"：设置抽屉里那一组阅读排版偏好（行高 / 三级标题字号 / 代码字号 / 标题对齐 /
正文字体 / 读取系统字体 / 强制动效）从"控件在位"变成**改了确实生效**，一个探针里连断五段：
驱动控件 → CSS 变量真的变了 → 读数变了 → localStorage 落了 → 「恢复默认」退回去 →
**另起一趟把偏好预先写进 localStorage，验刷新后 `apply()` 仍然应用**。套件 114→145 断言 / 111s。

为此给 `geom.mjs` 加了 `KB_GEOM_INIT`（页面任何脚本之前注入，与 shot.mjs 的 `init` 同源）——
没有它就测不了"刷新后仍然生效"，而那恰好是历史上真出过事的一步（B18 签名失效）。

**两条踩坑（都会让人误判"产品坏了"）：**
1. CSS 变量要读**内联**（`documentElement.style.getPropertyValue`），别读 `getComputedStyle` ——
   后者把 `var(--f-body)` 展成真实字体栈，第一轮我就把"字体其实生效了"读成了没生效。
2. 无头 Chrome **有** `window.queryLocalFonts`，但调用被拒（`SecurityError` → toast
   「读取系统字体失败（可能被拒绝授权）」）。所以断言只能要求"点了必有反馈"
   （枚举出分组 **或** 给出失败提示），静默才算坏；不要按"有没有 API"分支，那条在这里是反的。

**定点变异两发，都验证这套锁值得装**：
摘掉 `s.setProperty("--kb-line", …)` → 4 条红（含"读数变了但变量没变"这种最阴的情况）；
把 boot 里的 `prefs.apply()` 注释掉 → 4 条红，而**驱动类断言全绿** ——
也就是说只测"改了有没有反应"会完全放过"刷新后回退"这个真实故障模式。

### 再补 · 轮次 27（收件箱归档/忽略 2 行 + 修掉「忽略整个目录」点了没效果）

收件箱探针加第三段（勾两篇 → `.sel`/计数/按钮三态 → 批量归档填目标目录 → **两篇真的落盘** →
单篇 move 同理 → 根级/目录级"忽略"各走一条），顺手抓到一条**今天就坏**的功能错误（§6 第 34 行）：

**「忽略整个目录」完全没效果** —— 行不消失、`inbox-ignore.json` 里躺着一匹配不到的假规则、
toast 还理直气壮写"0 篇从列表移除"。根因是**三层对同一个 API 的契约各说一套**：

- `store.add_inbox_ignore` 要求"rel 本身就是目录"（注释原话），前端也照这个发；
- 路由却在中间又 `rsplit` 取了一层父目录 —— 同一个动作算了两遍，于是"忽略 `_inbox/开发产物`"
  记下的是它的**上一级**；根级文件更糟：`scope` 被静默改成 `file`，记下一条名叫目录名的文件规则；
- 前端第二处错：`rowsInDir(d.ignored)` 里 `d.ignored` 已是全路径，函数又拼一次 `_inbox/` 前缀
  → 前缀成了 `_inbox/_inbox/…` → 就算规则记对了，DOM 里的行也不会消失；
- 前端第三处是**说谎**：根级文件也弹「忽略整个目录？」并渲染成 `_inbox/文件名/`（多一个不存在
  的尾斜杠、承诺"以后扫进该目录的文件"并不成立）；而那句 `if (!dir)` 守卫是死分支，永远不为空。

修法：契约定为「`path` 就是要被忽略的东西本身」，路由不再上溯父目录 + 新增"不许忽略整个 `_inbox`"
的 400；前端按行的实际形态发 `scope`、去掉重复前缀、根级文案改说「忽略这个文件？」。
锁：e2e 两条（dir 记的就是我给的那个目录 / 忽略 `_inbox` 被拒）+ 行为套件四条
（含**重新拉服务端列表确认规则对下次扫描生效** —— 只看 DOM 会放过"记错规则"这一整类问题）。
定点变异：把路由退回旧实现 → e2e 1 条红 + 行为 2 条红。

**给下一位的提醒**：`/api/inbox/ignore` 的契约现在写在路由 docstring 里，
`store.add_inbox_ignore` 的注释与它一致；以后再加 scope（比如"按后缀忽略"）要**三处一起改**，
而这三处不一致正是本条缺陷的成因。

套件 145→160 断言 / 139s；e2e 196→198；台账 §0 轮次 27（199/166/33）、§6 第 34 行。


---

## 再补 · 轮次 28（同一会话继续，用户说"继续"）

§2 又收掉 6 行 `部分`（首页今日卡 / 术语过滤 / 术语分桶·子域·内联详情 / 串学漫游 /
模拟面试+精确命中 / 治理孤儿折叠·豁免）→ 台账 §0 轮次 28（**199 / 172 / 27**）。
第 13 套 160→**229 断言 / 176s**。

**这轮最大的收获是一条"从上线起就没工作过"的功能**：点「随机抽题开考」以后页面永远停在
「随机抽题中…」。原因不在数据、不在网络，在一句 `el.stage.innerHTML = ...` ——
`#kb-stage` 里同时躺着**常驻**的答题卡 `#kb-card`、CTA `#kb-learn-cta`、评分区 `#kb-grades`
和底部条 `#kb-learn-foot`，`startMock` / `showDone` / `showMockReport` 三处都往这个容器上
覆写消息，等于把常驻层炸掉；而 `el.*` 那批引用在模块初始化时就绑好了，于是 fetch 回来后
`showCard()` 照常执行、照常写 `el.term.textContent`，**全写在一个脱离文档的孤儿节点上**，
画面永远不动。修法是加一个独立消息层 `stageNote()`（插在 stage 前面，只切 `hidden`，
永不销毁常驻节点）。**给下一位的通则**：一个容器里既放常驻骨架又放临时消息时，
临时消息必须有**自己的子容器**；`innerHTML = ` 覆写一个含有已缓存引用的节点，
就是给自己埋一个"代码在跑、画面不动"的坑。

顺带两条：
1. **治理页「展开全部」在"扫出来就是 0 篇孤儿"时是个死钮**（`renderOrphans` 空列表提前
   `return`，控件显隐的赋值写在 return 之后；模板又没有 `hidden` 初值）。逐条豁免清空时
   反而不显 —— 上一轮（还剩 1 篇时）已经把 `hidden` 设过了。**一条路径靠侥幸盖住另一条**，
   所以这档必须**单独造**：用 `KB_GEOM_INIT` 在页面脚本之前把全量孤儿路径灌进
   `localStorage['kb-gov-orphan-whitelist']`，首屏扫描就直接进空分支。
2. **自家工具会吞掉真相**：`scripts/agent/geom.mjs` 原来只判 `value === undefined`，
   而 Promise reject 的 `Error` 经 `returnByValue` 反序列化就是 `{}`（message/stack 不是
   可枚举自有属性）—— 表达式一抛，测试侧拿到一个空 dict，28 条断言集体假红且**没有任何
   错误文本**。现在认 `exceptionDetails` / `subtype==='error'` 并回吐错误。**教训**：
   探针 harness 不许把"抛异常"和"返回空值"混成同一种输出。

**测 UI 交互前要先补的三个靶子**（P5 那份 6 篇语料进不去这些档）：34 篇孤儿 md
（治理折叠线写死 30 篇）、第二个 baike 词条带 `## 相关术语`（串学要有真路径 + 一个查不到
卡片的断头术语）、第三个词条放进另一个子域（子域筛选才有东西可筛）。

定点变异 4 处，各咬各自那条：learn.js 退回炸 stage → mock **9 条红**（含「#kb-card 仍在
文档里」）；governance 两处退回 → 1 条红；home.js 定义钮文案恒定 → 1 条红；
glossary.js 去掉空桶守卫 → 1 条红。P5 同批 **42/42 绿、零像素漂移**（DOM 重构没动画面，
`tests/ui-baselines/` 一个字没刷）；`test_reader 82 / e2e 198 / invariants 71 / js_props 47 /
learn 255 / predicates 89 / properties 15 / known_defects 6` 全绿，`ruff` All checks passed。

**§2 还剩 25 行 `部分`**（轮次 29 已收 9 行，见文末），分四堆：小说工具栏 4（章评 / TTS / 自动滚动 / 下载）、
设置·小说 6 + 快捷键 1、读数头 crumb 与右栏 6、统计页 3 + 标签合并流程 1 + 其它 4。
下一轮建议先做"统计页 3 行"（要先往 reading.db 灌事件才有画面，得先想清楚造数据的方式），
或者直接收"设置·小说 6 行"（机制与轮次 26 的排版偏好同源，`KB_GEOM_INIT` 现成可用）。


---

## 再补 · 轮次 29（同一会话继续）

§2 一次收 9 行（文档页那一堆）→ 台账 §0 轮次 29（**199 / 181 / 18**），套件 229→**261 断言 / 293s**。

**第 37 条缺陷：打开任何一篇文档，左树都不标"你在这儿"。** 判高亮的表达式是
`CUR.name === doc.name`，可是同一个"文档名"在项目里有**三种表示**：URL 段（`alpha`）、
`DOC.name`（`alpha.md`，服务端从 URL 尾巴直接给）、树节点的 `doc.name`（`alpha`，
因为链接要 `docUrl(x + ".md")` 再被 `docUrl` 削掉后缀）。等号两边跨了两种表示 → 恒不命中。
最妙的是它**半坏**：SPA 切页那一路是对的（`navigate()` 从 location 段取 name，那时不含扩展名），
所以"点着走都有高亮，一刷新就没了"。定点变异把判据退回原样，**只有"直载"那条断言红**，
换页那条照绿 —— 这就是"一条路径的侥幸盖住另一条路径的失效"（与轮次 28 那条死钮同形状）。
修法：**拿链接比链接**（两边都过同一份 `docUrl` 规范化），别再比三种名字表示。

**一条方法论上的坏消息，必须写下来**：这条修复在 P5 里是**零像素**的 —— 截图矩阵从不展开
二级子域（`kb-sub-open` 是独立于 `kb-tree-open` 的钥匙），文档行根本不在镜头里。
所以"基线没红"这次**完全不能**当作"没改到画面"的证据。要肉眼确证，得单独用
`shot.mjs --batch` + `init` 把 `kb-tree-open`/`kb-sub-open` 都灌上再截（`.qa/tree_open.png`：
强调色底 + 左边框 + 图标上色，三样都在）。**推论**：P5 的覆盖面等于"矩阵里被展开/被点开的那部分"，
凡是"要先把容器展开才看得见"的状态，像素基线一律瞎 —— 这类只能靠 DOM 断言（就是本轮这种）。

`#kb-toc-spark`（目录火花）是轮次 21 主动放弃的一格（读 reading.db、像素里被 FREEZE_CSS 隐掉），
本轮**捡回来了**：数据用应用自己的 `/api/track` 造（不手改派生库、不碰真实语料），
空态那一档用 `KB_GEOM_INIT` 只把 `/api/learn/recent_read` 这一条 URL 打桩成全 0 ——
一个探针同时锁住"有数据时画得出、7 根柱子的数字与接口逐项对齐"和"全 0 必须说话"。

定点变异 5 处：左树按名字比 → 1 红；`toggleDocMark` 的 `on: !cur` 反了 → 2 红；
右栏 `editable` 恒真（美化版会长出编辑/删除入口）→ 2 红；目录火花全 0 判据摘掉 → 1 红；
`jumpToTagEdit` 不展开输入行 → 2 红。P5 复跑 42/42 绿（没刷基线）。

还有一件值得单独记的：**这次是自家门禁红给我看的**（§6 第 38 行）。`test_js_props` 的 TOC 探针在
pre-commit 里红 2 条、我单独连跑两次 47/47 全绿。根因是探针把「滚一格等 600ms 量一次」当成了契约，
可那支目录高亮只由 IntersectionObserver 驱动（`app.js:753`），合成 scroll 事件它根本不监听，
回调落在第几帧看机器负载 —— 钩子里前面刚跑完三套 Python smoke，600ms 就是不够。
已改成**轮询到收敛**（3s 超时）并把 `vw/vh/scrollHeight/clientHeight` 一起回吐。
反向验证过：把 `tocSync` 判据改成永不动 → 2 条红且每格 `waited: 3000` 打满超时，
说明"等收敛"没把它惯成恒真。**通则**：凡由 IO / rAF / 异步回调驱动的状态，探针必须轮询到收敛，
固定 sleep 只测"我等够了没有"，不测"它到不到得了"。

**§2 还剩 16 行 `部分`**：设置·小说 6 + 快捷键 1、小说工具栏 4、统计页 3、标签合并流程 1、顶栏 #toast 1。
下一轮建议直接收"设置·小说 7 行"——机制与轮次 26 的阅读排版偏好同源
（滑杆 → CSS 变量 → localStorage → 刷新仍生效），`KB_GEOM_INIT` 现成可用；
统计页那 3 行的做法本轮已经铺好（`/api/track` 造数据 + 与接口逐项对账）。
