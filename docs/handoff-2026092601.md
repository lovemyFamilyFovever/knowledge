# Handoff · 轮次 38：全项目逐文件排查与清理（2026-09-26 第 1 份）

## ① 我们在做什么任务

用户要求：**把项目里每一个文件都排查一遍**（除 `node_modules/`），然后把所有临时文件 —— 一次性脚本、
测试残留、日志、临时截图、临时文档 —— 全部删掉。明确"不在乎多长时间，但必须每个文件都排查清楚"。
这是彻查（P0~P9）之后的收尾轮，记为轮次 38，台账在 `测试文件/覆盖台账.md`。

## ② 已经完成了什么

**全量底账**：`.cleanup/inventory.py` 扫出 **5074 个文件**（除 node_modules 与 `.git/` 内部），
每个都带 路径/字节/行数/mtime/git 状态/类型。`.cleanup/classify.py` 把每个文件切成三段并**断言强制对账**：
A（已授权直删）3071 + B（待批）133 + C（保留）1870 = 5074。逐文件判据在 `.cleanup/verdicts.tsv`。

**判据来源**（可复跑）：`.cleanup/refs.py` 全仓文本引用反查；仓库自带的 `scan_dup.py`（残留副本 0）、
`scan_fm.py`（checked 1126、污染 0）、`check_frontmatter.py`（严格 YAML）；我另加的逐字节 SHA256 重复扫描；
以及 `npx stylelint --print-config`（拿工具自己的输出当判据）。

**A 段已删**：`.qa`(2753) / `.qoder-credits`(148) / `qwenwork`(101) / `.box-agent`(3) / `.ruff_cache` /
`__pycache__`(91) / `indexes` / `backups` → 应删 3070、实删 2998；剩下 72 个是**随后跑门禁自动长回来的**
缓存（`__pycache__`、`.ruff_cache`、`.qa/gate/*.log`、`indexes/index.db` 21MB），
外加 1 个被别的进程占用、我没有强杀的 `.qa/q29f.log`。

**B 段经用户逐条批准后执行**：`git rm` 13 个跟踪文件（7 个一次性脚本、`docs/BACKLOG.md`、
2 份 refactor `.log`、`static/echarts.min.js`、无引用的 `CormorantGaramond-500i.woff2`、
**从未生效的 `stylelint.config.mjs`**）；content 里删 63 个（50 份确有第二份的 `_inbox/repos` 重复文档、
3 份自记办完的作业纸、10 个 `_trash` 测试残留）。

**删前救下 58 份**（关键）：见 ⑤ 第 1 条。

验证：删完 `python app/app.py --import-check` OK；完整 `sh .githooks/pre-commit` all green；
`npm run lint:css` 仍 0 告警（正好证明删掉的那份配置确实没在跑）。

## ③ 当前卡在哪

没卡。两件如实留着的：

1. `.qa/q29f.log` 删不掉（WinError 32 被别的进程占用）。**没有杀任何进程** —— 那是别的会话的文件句柄。
   下次它空闲时 `rm .qa/q29f.log` 即可。
2. `content/_inbox/repos/work/AIjin/` 还剩 58 份 md（全库仅此一家，`_inbox` 不进 git）。
   已在根 `BACKLOG.md` 记为待办，与既有的「repos 侧 108 份文档导数据」同件事，**须由用户自己整理完再定去留**。

## ④ 下一步计划

- 用户自己整理 `_inbox/repos` 那 58 份（或指示我按某种规则归档进 `content/projects/AI金/`）。
- `stylelint` 若要"变严"，需要一次独立改动：把原 mjs 里 `no-duplicate-selectors`/`block-no-empty`
  设 true 的思路并回 `.stylelintrc.json`，先跑 `npm run lint:css` 看红多少再修（已记 BACKLOG）。
- 本轮清理**尚未 push**（提交纪律：push 由用户执行或明确授权）。

## ⑤ 踩过的坑（绝对不要再踩）

1. **批准也是会错的，尤其是被我的判据带偏的那种**。用户批"全删 121 个"是照着我说"与已归档正本逐字节重复"
   批的，而那句话是从 **50 组重复样本外推到 108 份整体**的以偏概全；动手前逐份哈希回查才发现
   58 份全库仅此一家，而 `_inbox` 被 .gitignore 排除 —— 删了连 git 历史都回不来。
   **规则：删任何 gitignore 覆盖位置里的文件前，必须把"有第二份"从我的断言变成机器的当场核对。**
2. **整目录 rmtree 是陷阱**。`indexes/scripts/tests/app` 这些顶层目录下也有 A 段文件（`__pycache__`），
   我第一版按"顶层目录名"整目录删，差点把源码目录连锅端；当场改成 `.cleanup/apply_a.py` **逐文件删 + 护栏**
   （名单里出现 `app/`、`tests/`、`static/` 等前缀且不是缓存 → 直接 exit 1）。
3. **删完的"没删干净"多半是自动长回来的**。72 个 A 段残留不是漏删，是我随后跑门禁重新生成的缓存；
   对账脚本要显式区分"没删"与"删了又长出来"，否则会把自己的验证动作误报成失败。
4. **Windows 保留设备名删不掉**：`.qa/ui-demo/mobile-ui/nul` 普通 `unlink` 报 WinError 5，
   要走 `\\?\` 扩展路径。清理这类目录时记得加这条兜底。
5. **"文件存在"与"配置生效"都不等于事实**：`echarts.min.js` 躺在 `static/` 里、还在 `check_dangling_tokens.py`
   的跳过名单中出现，看起来像在用；`stylelint.config.mjs` 写得比生效的 `.stylelintrc.json` 更严，却从没被读过
   （`.stylelintrc.json` 在发现顺序里优先）。工具链判据要用 `--print-config` / 引用反查这类**工具自己的输出**。
   台账 §6 第 51 行。
6. **删跟踪文件前先查"它里面有没有唯一未完成的事"**：`docs/BACKLOG.md` 是过期文件，但里面那条
   「_inbox 抓取对超长文的硬截断」是唯一没做完的待办，且 `writing-spec-v1.2.md` 有两处指针指向它。
   先把条目搬进根 `BACKLOG.md`、改掉指针，再删文件 —— 顺序反了就是丢信息。

## 项目速览 / 相关文件索引

- 底账与工具（终检后删）：`.cleanup/inventory.py`、`refs.py`、`classify.py`、`apply_a.py`、`apply_b2.py`、
  `migrate38.py`、`docs38.py`、`清理清单.md`、`verdicts.tsv`、`inventory.tsv`、`b2-result.json`
- 台账：`测试文件/覆盖台账.md` §0 轮次 38、§6 第 51/52 行、§1/§10.1 的 api_patrol 指针、§14.1 计数
- 上一轮：`docs/handoff-2026092503.md`（CI 首次全绿与 annotation，轮次 32~37）

## 给新会话的第一句话

接手"全项目文件排查与清理"：先读 `docs/handoff-2026092601.md` 的 ⑤ 第 1、2 条，
再跑 `python -X utf8 .cleanup/classify.py` 重算 A/B/C 对账（如果 `.cleanup/` 还在）；
**未经用户逐条批准不要删 `content/` 下任何东西**，`_inbox` 里删任何文件前都要当场做"是否有第二份"的哈希核对。
