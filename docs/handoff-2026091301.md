# Handoff 2026-09-13 — 光标还原 + 验证债清零 + CRLF 修复 + hero 空隙修复（进行到一半被交接）

> 面向零上下文的新会话。先读本文，再读 `AGENTS.md`。
> 前序：`docs/handoff-2026091203.md`（22 项 bug 修复收官）。
> 本会话工作区另有 5 笔新 commit，**均未 push**。

---

## ① 本会话在做什么任务（时间线）

1. **读 `handoff-2026091203.md` 并逐条对账**（用户要求「谈谈想法」）
   - 实测推翻其两条主张：`.python/` 在本机**存在**（handoff 写于另一台机器，没注明机器名）；
     「Agent 架构模式详解.md 未提交污染」已被 `e9a1f99` 规整提交
2. **巡检资产复跑抓到真 bug**（commit `4334f25`）：
   - `_TAGS_INLINE_RE` 行尾锚 `[ \t]*$` 不容忍 `\r`，真实语料 690/690 全 CRLF →
     **标签合并在本机语料上 100% 失效**（n_skipped=385）
   - 为什么五套测试没测出：test_govern 夹具的 tags 恰是 frontmatter **末行**，
     `$` 命中串尾侥幸通过；真实语料 tags 行后面还有 source/collected 等键
   - 修复 = 前瞻锚 `(?=\r?$)`（不消费 `\r`，字节保真不破坏）；
     `tests/test_govern.py` 新增 `test_merge_tag_crlf_bytes` 回归锁
3. **api_patrol 适配**（commit `4237e4d`）：探针名去 `_` 前缀（B22 新守卫拒绝 `_` 目录）、
   tag 注入改为替换 stamp 的 `tags: []` 行（原逻辑另插一行造成重复 YAML 键）。22/22 全绿
4. **用户要求还原鼠标样式**（commit `6ac06ba`，同笔收口用户本人的 4 处改动）：
   - 根因：T0 换皮引入 `cursor.js`（dot+ring 跟随光标）+ `html.cursor-on *{cursor:none !important}`
   - 修复：base.html 摘加载、motion.js 删 initCursor 委托+启动调用+导出表引用、
     style.css 删光标样式块、cursor.js 移入 `content/_trash/20260913-cursor-removed/`
   - 验证：探针实测 cursor-on=false、dot/ring/label=0、KbCursor=undefined
   - 同笔包含用户改动：app.py `--dev` 热重载参数、删两个旧题库 md、prototype.html、
     package.json/pyrightconfig.json
5. **验证债清零**（handoff-2026091203 留的）：B4 双链可点化/锚点/复习卡跳转全部浏览器探针实测通过
6. **残留 2 收口**（commit `b3b7eda`）：saveDoc frontmatter 安全网 CRLF 归一比对。
   死因实锤：FM_RAW 含 `\r`（/raw 原文）而 textarea 值是 LF，`head === ED_INITIAL_HEAD` 永假。
   修复 = 比对前两侧 `\r\n`→`\n` 归一，还原时仍用 FM_RAW 原文。APP_JS_VERSION 24→25
7. **工具链收口**（commit `bd6c6f2`）：用户落盘的 eslint/stylelint flat config、
   package-lock、start-dev.bat、.gitignore node_modules（实测 npm run lint:js = 0 errors/53 warnings）
8. **用户报「刷新后红框区域撑起来」**（进行中，**只修了一半**，见 ③）

## ② 已经完成了什么

### 提交链（本会话 5 笔，全部 pre-commit 五套测试绿，**均未 push**）

```
bd6c6f2 chore(toolchain): ESLint/stylelint 回归安全网 + start-dev.bat
b3b7eda fix(editor): saveDoc frontmatter 安全网 CRLF 归一比对——死逻辑复活
6ac06ba feat(dev+ui): --dev 热重载、语料清理、工具链配置、还原系统原生光标
4237e4d chore(patrol): api_patrol 适配 B22 新守卫与真实 save stamp
4334f25 fix(store): merge_tag 行尾锚容忍 CRLF——真实语料 690/690 曾全库跳过
```

### 测试基线

test_reader 70 / test_new_project 9 / test_rag OK / test_govern OK（含新 CRLF 回归锁）/
test_learn 208 — 全绿。api_patrol 22/22、search_patrol 10/10。

### 验证债状态（handoff-2026091203 ③ 的四条）

- ① 双链可点 ✓（摘要页 11 链全渲染为 a.wikilink）
- ② 未解析双链样式 ✓（0 dead；warn 虚线逻辑在 app.js decorateWikilinks）
- ③ 复习卡锚点跳转 ✓（Trie 卡 anchorEl=true、elTop=125；费茨定律卡 #常见误区 落点截图同证）
- ④ 复习空态 ✓（learn.js:281 first 分支「暂时没有可学的卡」；本机已有 1146 卡无法复现空态，以单测佐证）

## ③ 当前卡在哪：用户报的「红框区域撑起来」只修了一半

**用户两连报**（附截图）：
1. 阅读页 topbar 下方红框区域刷新后「撑起来」
2. 首页「很多元素往下移动一段距离」+ 大标题「知 库」中间大空隙 +
   tagline「把 散落 的 知识， 接回 一个」字间断裂 + metrics 行整体下移

**已复现、已定责（探针实测）**：
- `.hero-tagline` 高 84px（正常应 ~40px）：模板 7 个 `<span data-reveal>` 之间有
  **真实空格 + 8 个前导缩进空格 + 换行**，`home.css:21` 把 `[data-reveal]` 设为
  `display:inline-block` → 空白全部渲染成可见空隙 → tagline 折行 →
  **metrics/今日学习整体被顶下去**（用户红框②的全部内容）
- spec 原文（docs/superpowers/T2 任务）：「把散落 8 年的知识，接回一个入口」
  逐词 reveal 靠 span 边界，**词间不应有空格**
- 另：gsap revealGroup 用 fromTo(opacity:0, y:40)，ScrollTrigger progress 卡 0.29 的
  中间态在探针虚拟时间下可见；真实浏览器是否也卡需在修复后复验

**已完成**：`app/templates/home.html` tagline 已改为零空白连接
`<span data-reveal>把</span><span data-reveal>散落</span>...`（已拷回+渲染产物验证）。
**注意：这一改动在工作区未提交**（`git status` 里 home.html 是 M）。

**未完成**：
1. motion.js `revealGroup` 的中间态兜底没做完——原计划：初态 y:40/opacity:0 只在
   ST 触发就绪后才设置（或改 to() + CSS 初态），防止字体加载/布局抖动时元素
   带着位移中间态折行。**motion.js 本次会话还没被再次修改**（上一次修改是删 initCursor，
   已提交），动手前先 `git diff static/motion.js` 确认现场
2. 修复后必须浏览器探针复验：tagline 单行、无空隙、metrics 回位、
   「知 库」hero-title 空隙是否也消失（hero-title 的 span.line 之间无空白，
   若仍有隙则查 .hero-title letter-spacing:-.04em 与 font fallback 的组合，
   另怀疑 --disp 变量指向的字体在中文下度量异常——**未查完**）
3. 用户第一张截图（阅读页 topbar 下红框）**尚未定责**：多视口探针均未复现出独立占位元素，
   pixel 采样到色带 y25~100(2x) 含 --acc 渐变。两个可能：与首页同源（tagline 类似的
   中间态/字体回退在 workbench 顶部工具条的体现），或浏览器缓存旧 CSS。
   修复首页后让用户刷新验证是否同源消失，不要过早修
4. `docs/adr-2026-09-13-架构决策记录.md`（codebase-memory MCP 生成）未跟踪，
   是否入库**等用户拍板**，不要擅自提交

## ④ 下一步计划（按序）

1. **收尾 hero 修复**：复验 tagline 单行/无空隙/metrics 回位（模板改动已就位，
   探针页 `_home_probe2.html` 在 `content/_inbox/`，测完移 `_trash/`）
2. 决定 motion.js revealGroup 是否需要中间态兜底：先在真实浏览器（非虚拟时间）
   确认动画正常播完则**不加**（避免过度工程）；若确认卡中间态再加
3. 让用户硬刷新（Ctrl+F5）确认阅读页红框是否消失（CSS 缓存嫌疑最大；
   若仍在，抓取他的 viewport 尺寸与 UserAgent 再复现）
4. 提交 home.html 修复（pre-commit 全绿后），commit message 建议第一行：
   `fix(home): hero-tagline 逐词 span 模板空白致字间断裂与 metrics 下移`
5. **push 全部 6 笔**（5 笔 + 本笔），push 纪律是用户手动
6. `term_collisions` 决策仍待用户：先看 `/api/learn/sync` 上报的碰撞数字再定，
   不改 card_id 除非碰撞规模大到值得做迁移映射（新会话别自作主张改键方案）

## ⑤ 踩过的坑（本会话新增 + 沿用）

### 本会话新踩

1. **夹具形态掩盖正则盲区**：test_govern 夹具 tags 恰为 FM 末行，`$` 命中串尾侥幸绿；
   真实语料 tags 后还有其他键 → CRLF 盲区全量暴露。教训：**行级正则的测试夹具
   必须包含「目标行后面还有内容」的形态**
2. **handoff 不写机器名会误导**：`.python/` 存在与否两台机器相反。**交接文档必须注明
   「哪台机器、哪个检出」**
3. **探针 iframe 视口≠用户真实视口**：复现布局问题先用用户截图的精确尺寸
   （1200×599），再用全页直截对照；两者都失败时转向像素采样取证
4. **headless --screenshot 首拍可能空转**（探针 setTimeout 5s > 虚拟时间预算消耗速度）：
   等 2s 重查再重试；结果字段被虚拟时间裁切时，把输出渲染成大字号固定定位元素再截图
5. **编辑按钮无 id**（`onclick="openEditor()"`），DOM 探针直接调 `w.openEditor()` 即可
6. **span 间模板空白 + inline-block = 可见空隙**：逐词 reveal 类动画的模板必须
   零空白连接 span（Jinja 注释 `{# #}` 说明为什么）

### 沿用必读（前序 handoff 的坑全部仍有效）

- Windows `write_text` 双写 `\r\r\n`；字节保真用 read_bytes/write_bytes
- 静态推断前先探针实测；报告区分「实测/静态」证据
- 回归探针文件放 `_` 前缀目录，不进树/索引；用完即清
- 并行会话纪律：动手前 git log/status 看现场；**只 stage 自己碰过的文件（禁 -A）**；
  行号会漂移，定位用 grep 锚点
- 本会话实操补充：工作区会持续出现**用户本人/其他工具**落盘的改动
  （本轮：app.py --dev、eslint 配置、ADR、package.json 两轮变化）——
  每次提交前重新 `git status`，逐个甄别来源，用户的改动**问一句或原样收口**，
  绝不混进自己的 commit

## 项目速览

个人知识库单一入口：`content/` md/html 唯一事实源，Flask 本地阅读器（端口 5001）+
FTS + 本地 RAG + SM-2 复习系统。无构建工具、无 CDN、语料不出网。`indexes/*.db` 派生缓存。
本机（DESKTOP-EOA6EJ4）有 `.python\` 便携运行时（3.13.1）；写 handoff-2026091203 的
那台机器没有——**两机并行开发，git 是唯一同步通道**。

## 关键约定（增量）

- AGENTS.md 不变量照旧；新增强护栏（handoff-2026091203）：写回禁整块重建、
  不可见文件谓词 `is_visible_doc` 四处共用、move 级联四件套
- `APP_JS_VERSION` 当前 **25**（app.js 变更必须递增）
- pre-commit 五套测试：test_reader(70) / test_new_project(9) / test_rag / test_govern / test_learn(208)
- 巡检脚本：`scripts/api_patrol.py`（22 断言）+ `scripts/search_patrol.py`（10 断言），
  大改动后必跑；探针文档自动清理入 `_trash/`
- 服务启动：生产 `python app/app.py`；开发 `start-dev.bat`（--dev 热重载）

## 相关文件索引（本会话涉及）

- `app/store.py` — _TAGS_INLINE_RE 前瞻锚修复（4334f25）
- `app/templates/home.html` — tagline 零空白连接（**工作区未提交**）
- `static/app.js` v25 — saveDoc 安全网归一比对（b3b7eda）
- `static/motion.js` — initCursor 已删，五配方（6ac06ba）
- `scripts/api_patrol.py` / `scripts/search_patrol.py` — 巡检断言（4237e4d 前后）
- `docs/adr-2026-09-13-架构决策记录.md` — 未跟踪，待用户拍板
- `content/_trash/20260913-cursor-removed/` — cursor.js 留档 + 本会话全部探针页

## 给新会话的第一句话

> 先读本文和 `docs/handoff-2026091203.md`。工作区里 `home.html` 有未提交的 tagline
> 修复（已验证渲染产物），先按 ④1 复验截图，然后按序推进；`motion.js` 本次还没动，
> `git diff` 确认现场再改。ADR 文件是用户的，别动。
