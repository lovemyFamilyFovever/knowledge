---
title: "工程化与工具链面试题库 - 45道精选题目"
tags: []
source: "baike"
source_path: "技术题库 / 工程化与工具链"
collected: "2026-09-05"
status: "imported"
---

工程化与工具链面试题库 - 45道精选题目
🛠️ 工程化与工具链面试题库
45道精选题目 - 从基础到高级的全面覆盖
📚 45道题目
🎯 详细解答
💡 实战技巧
📱 响应式设计
📦 Webpack
⚡ Vite
🔧 Babel
🔍 ESLint
🧪 测试
🚀 CI/CD
📦 Webpack
1. Webpack是什么？它有哪些核心概念？
简单
Webpack
构建工具
模块打包
查看答案
回答模板1：概念解释
Webpack是一个现代JavaScript应用程序的静态模块打包工具。它能够处理JavaScript、CSS、图片等资源。
核心概念：
1.
Entry
：入口文件
2.
Output
：输出配置
3.
Loader
：处理非JavaScript文件
4.
Plugin
：扩展功能
5.
Mode
：开发/生产模式
回答模板2：配置示例
// webpack.config.js
const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
    entry: './src/index.js',
    output: {
        path: path.resolve(__dirname, 'dist'),
        filename: 'bundle.[contenthash].js',
        clean: true
    },
    module: {
        rules: [
            {
                test: /\.js$/,
                exclude: /node_modules/,
                use: 'babel-loader'
            },
            {
                test: /\.css$/,
                use: ['style-loader', 'css-loader']
            }
        ]
    },
    plugins: [
        new HtmlWebpackPlugin({
            template: './src/index.html'
        })
    ],
    mode: 'production'
};
回答模板3：使用场景
Webpack的使用场景：
1.
模块打包
：将多个模块打包成一个文件
2.
代码分割
：按需加载代码
3.
资源管理
：处理CSS、图片等资源
4.
开发工具
：热模块替换、开发服务器
5.
优化
：压缩、Tree Shaking等
2. Webpack中的Loader和Plugin有什么区别？
中等
Loader
Plugin
Webpack
查看答案
回答模板1：核心区别
Loader和Plugin的主要区别：
1.
作用
：Loader处理文件转换，Plugin扩展功能
2.
配置
：Loader在module.rules中配置，Plugin在plugins中配置
3.
执行时机
：Loader在模块加载时执行，Plugin在生命周期中执行
4.
功能
：Loader只能做文件转换，Plugin可以做任何事情
回答模板2：使用示例
// Loader使用
module: {
    rules: [
        {
            test: /\.js$/,
            use: 'babel-loader'
        }
    ]
}

// Plugin使用
plugins: [
    new HtmlWebpackPlugin({
        template: './src/index.html'
    }),
    new MiniCssExtractPlugin({
        filename: '[name].css'
    })
]
回答模板3：选择建议
选择建议：
1.
使用Loader
：需要转换文件内容时
2.
使用Plugin
：需要扩展Webpack功能时
3.
自定义Loader
：有特殊的文件转换需求
4.
自定义Plugin
：需要介入Webpack生命周期
5.
参考文档
：查看官方文档选择合适的Loader/Plugin
3. 如何优化Webpack构建性能？
困难
性能优化
构建速度
Webpack
查看答案
回答模板1：基础优化
Webpack基础性能优化：
1.
使用缓存
：持久化缓存
2.
减少文件搜索
：缩小文件搜索范围
3.
使用多线程
：thread-loader
4.
使用预编译
：HardSourceWebpackPlugin
回答模板2：高级优化
// 配置优化
module.exports = {
    // 持久化缓存
    cache: {
        type: 'filesystem',
        buildDependencies: {
            config: [__filename]
        }
    },
    
    // 缩小搜索范围
    resolve: {
        extensions: ['.js', '.jsx', '.ts', '.tsx'],
        modules: [path.resolve(__dirname, 'src'), 'node_modules']
    },
    
    // 使用thread-loader
    module: {
        rules: [
            {
                test: /\.js$/,
                use: [
                    {
                        loader: 'thread-loader',
                        options: {
                            workers: 3
                        }
                    },
                    'babel-loader'
                ]
            }
        ]
    }
};
回答模板3：最佳实践
Webpack性能优化最佳实践：
1.
开发环境
：使用cheap-module-source-map
2.
生产环境
：使用TerserPlugin压缩
3.
代码分割
：splitChunks配置
4.
Tree Shaking
：移除未使用代码
5.
使用DLL
：预编译不常变化的依赖
⚡ Vite
21. Vite是什么？它与Webpack有何不同？
简单
Vite
构建工具
对比
查看答案
回答模板1：概念解释
Vite是一个现代化的前端构建工具，它利用浏览器原生ES模块支持来提供快速的开发体验。
Vite与Webpack的区别：
1.
启动速度
：Vite按需编译，Webpack全量编译
2.
热更新
：Vite精确的HMR，Webpack全量刷新
3.
配置
：Vite开箱即用，Webpack需要配置
4.
生态
：Webpack生态更丰富
回答模板2：使用示例
// 创建Vite项目
npm create vite@latest my-app -- --template react

// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
    plugins: [react()],
    server: {
        port: 3000,
        open: true
    },
    build: {
        outDir: 'dist',
        sourcemap: true
    }
});
回答模板3：选择建议
选择建议：
1.
使用Vite
：新项目、追求开发体验
2.
使用Webpack
：老项目、需要复杂配置
3.
考虑团队
：团队熟悉哪种工具
4.
考虑生态
：需要的插件支持
5.
考虑性能
：构建速度要求
22. Vite如何处理依赖预构建？
中等
依赖预构建
Vite
性能优化
查看答案
回答模板1：概念解释
Vite的依赖预构建是指在开发服务器启动时，将node_modules中的依赖转换为浏览器可以运行的格式。
预构建的作用：
1.
转换格式
：将CommonJS转换为ESM
2.
减少请求
：合并小模块
3.
缓存
：预构建结果会被缓存
回答模板2：配置方法
// vite.config.js
export default defineConfig({
    optimizeDeps: {
        include: ['lodash', 'axios'],
        exclude: ['your-local-package'],
        esbuildOptions: {
            target: 'esnext'
        }
    }
});
回答模板3：最佳实践
依赖预构建最佳实践：
1.
排除本地包
：排除本地开发的包
2.
包含大依赖
：包含大型依赖
3.
清除缓存
：遇到问题时清除缓存
4.
监控构建
：监控预构建时间
5.
优化依赖
：减少不必要的依赖
23. Vite如何处理CSS和资源文件？
中等
CSS处理
资源文件
Vite
查看答案
回答模板1：CSS处理
Vite对CSS的处理：
1.
原生支持
：直接导入CSS文件
2.
预处理器
：支持Sass、Less、Stylus
3.
CSS Modules
：支持CSS Modules
4.
PostCSS
：支持PostCSS插件
回答模板2：资源文件处理
// 导入CSS
import './styles.css';
import styles from './styles.module.css';

// 导入图片
import logo from './logo.png';
const img = document.createElement('img');
img.src = logo;

// 导入JSON
import data from './data.json';

// 静态资源
const imgUrl = new URL('./logo.png', import.meta.url).href;
回答模板3：最佳实践
资源处理最佳实践：
1.
使用别名
：配置路径别名
2.
CSS变量
：使用CSS变量
3.
代码分割
：按需导入资源
4.
优化图片
：压缩图片
5.
使用CDN
：静态资源使用CDN
🔧 Babel
31. Babel是什么？它有哪些核心功能？
简单
Babel
转译器
JavaScript
查看答案
回答模板1：概念解释
Babel是一个JavaScript编译器，用于将现代JavaScript代码转换为向后兼容的版本。
核心功能：
1.
语法转换
：转换新语法为旧语法
2.
Polyfill
：添加缺失的功能
3.
JSX转换
：转换JSX语法
4.
TypeScript
：转换TypeScript
回答模板2：配置示例
// babel.config.js
module.exports = {
    presets: [
        ['@babel/preset-env', {
            targets: {
                browsers: ['last 2 versions', 'not dead']
            },
            useBuiltIns: 'usage',
            corejs: 3
        }],
        '@babel/preset-react',
        '@babel/preset-typescript'
    ],
    plugins: [
        '@babel/plugin-proposal-class-properties',
        '@babel/plugin-proposal-object-rest-spread'
    ]
};
回答模板3：使用场景
Babel的使用场景：
1.
浏览器兼容
：支持旧版浏览器
2.
新语法
：使用最新的JavaScript语法
3.
React
：转换JSX语法
4.
TypeScript
：转换TypeScript
5.
框架
：Vue、Angular等框架
32. Babel的预设（Presets）和插件（Plugins）有什么区别？
中等
预设
插件
Babel
查看答案
回答模板1：核心区别
预设和插件的主要区别：
1.
定义
：预设是一组插件的集合，插件是单个转换功能
2.
配置
：预设可以一次性配置多个插件
3.
执行
：插件在预设之前执行
4.
顺序
：插件执行顺序是从前到后，预设是从后到前
回答模板2：使用示例
// 使用预设
{
    "presets": [
        "@babel/preset-env",
        "@babel/preset-react",
        "@babel/preset-typescript"
    ]
}

// 使用插件
{
    "plugins": [
        "@babel/plugin-proposal-optional-chaining",
        "@babel/plugin-proposal-nullish-coalescing-operator"
    ]
}

// 预设包含插件
// @babel/preset-env 包含了 ES2015-ES2020 的所有插件
回答模板3：选择建议
选择建议：
1.
使用预设
：需要一组相关插件时
2.
使用插件
：只需要特定转换时
3.
自定义预设
：创建自己的插件组合
4.
参考文档
：查看官方文档选择合适的预设/插件
5.
性能考虑
：减少不必要的插件
33. 如何配置Babel支持TypeScript？
中等
TypeScript
Babel
配置
查看答案
回答模板1：基础配置
Babel支持TypeScript的配置：
1.
安装预设
：@babel/preset-typescript
2.
配置预设
：在babel.config.js中添加
3.
移除类型
：Babel只移除类型注解，不做类型检查
回答模板2：配置示例
// 安装
npm install --save-dev @babel/preset-typescript

// babel.config.js
module.exports = {
    presets: [
        '@babel/preset-env',
        '@babel/preset-react',
        '@babel/preset-typescript'
    ]
}

// tsconfig.json
{
    "compilerOptions": {
        "target": "ESNext",
        "module": "ESNext",
        "moduleResolution": "node",
        "jsx": "react-jsx",
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true,
        "noEmit": true
    }
}
回答模板3：最佳实践
TypeScript + Babel最佳实践：
1.
使用tsc检查
：Babel只转译，类型检查用tsc
2.
配置tsconfig
：设置noEmit: true
3.
开发流程
：开发时用Babel，构建时用tsc检查
4.
IDE支持
：配置IDE的TypeScript支持
5.
测试
：编写类型测试
🔍 ESLint
41. ESLint是什么？它有哪些核心功能？
简单
ESLint
代码检查
代码质量
查看答案
回答模板1：概念解释
ESLint是一个JavaScript代码检查工具，用于识别和报告代码中的问题。
核心功能：
1.
代码检查
：检查代码风格和潜在问题
2.
自动修复
：自动修复一些问题
3.
自定义规则
：创建自定义检查规则
4.
插件系统
：扩展功能
回答模板2：配置示例
// .eslintrc.js
module.exports = {
    env: {
        browser: true,
        es2021: true,
        node: true
    },
    extends: [
        'eslint:recommended',
        'plugin:react/recommended',
        'plugin:@typescript-eslint/recommended'
    ],
    parser: '@typescript-eslint/parser',
    parserOptions: {
        ecmaVersion: 'latest',
        sourceType: 'module'
    },
    plugins: ['react', '@typescript-eslint'],
    rules: {
        'indent': ['error', 4],
        'linebreak-style': ['error', 'unix'],
        'quotes': ['error', 'single'],
        'semi': ['error', 'always']
    }
};
回答模板3：使用场景
ESLint的使用场景：
1.
代码规范
：统一团队代码风格
2.
错误预防
：检测潜在的代码错误
3.
代码质量
：提高代码质量
4.
自动修复
：自动格式化代码
5.
CI/CD集成
：在构建流程中检查代码
42. 如何配置ESLint与Prettier集成？
中等
Prettier
代码格式化
集成
查看答案
回答模板1：基础配置
ESLint与Prettier集成的配置：
1.
安装依赖
：eslint-config-prettier、eslint-plugin-prettier
2.
配置ESLint
：禁用与Prettier冲突的规则
3.
配置Prettier
：创建.prettierrc配置文件
回答模板2：配置示例
// 安装
npm install --save-dev eslint-config-prettier eslint-plugin-prettier

// .eslintrc.js
module.exports = {
    extends: [
        'eslint:recommended',
        'plugin:react/recommended',
        'prettier' // 放在最后，覆盖其他配置
    ],
    plugins: ['prettier'],
    rules: {
        'prettier/prettier': 'error'
    }
};

// .prettierrc
{
    "semi": true,
    "trailingComma": "es5",
    "singleQuote": true,
    "printWidth": 80,
    "tabWidth": 2
}
回答模板3：最佳实践
ESLint + Prettier最佳实践：
1.
规则分工
：ESLint检查代码质量，Prettier格式化代码
2.
避免冲突
：使用eslint-config-prettier
3.
自动格式化
：配置编辑器保存时格式化
4.
CI集成
：在CI中检查格式
5.
团队统一
：统一团队的配置
43. 如何创建自定义ESLint规则？
困难
自定义规则
ESLint插件
高级配置
查看答案
回答模板1：基础概念
创建自定义ESLint规则需要了解ESLint的插件系统和AST（抽象语法树）。
创建步骤：
1.
创建插件
：创建ESLint插件
2.
定义规则
：编写规则逻辑
3.
配置规则
：在ESLint配置中使用
回答模板2：代码示例
// eslint-plugin-custom/rules/no-foo.js
module.exports = {
    create(context) {
        return {
            Identifier(node) {
                if (node.name === 'foo') {
                    context.report({
                        node,
                        message: '不允许使用foo变量名'
                    });
                }
            }
        };
    }
};

// eslint-plugin-custom/index.js
module.exports = {
    rules: {
        'no-foo': require('./rules/no-foo')
    }
};

// .eslintrc.js
module.exports = {
    plugins: ['custom'],
    rules: {
        'custom/no-foo': 'error'
    }
};
回答模板3：最佳实践
自定义ESLint规则最佳实践：
1.
了解AST
：学习JavaScript AST结构
2.
测试规则
：编写规则测试用例
3.
文档
：为规则编写文档
4.
性能
：优化规则性能
5.
维护
：定期更新规则
🧪 测试
44. 什么是单元测试、集成测试和端到端测试？
中等
单元测试
集成测试
端到端测试
查看答案
回答模板1：概念解释
三种测试类型的区别：
1.
单元测试
：测试最小的代码单元（函数、类）
2.
集成测试
：测试多个组件协作
3.
端到端测试
：测试整个应用流程
回答模板2：金字塔模型
// 测试金字塔
//        /\
//       /E2E\      少量端到端测试
//      /----\
//     /集成测试\    适量集成测试
//    /--------\
//   /  单元测试  \  大量单元测试
//  /------------\

// 单元测试示例
describe('add', () => {
    it('应该正确相加两个数', () => {
        expect(add(1, 2)).toBe(3);
    });
});

// 集成测试示例
describe('UserAPI', () => {
    it('应该创建用户', async () => {
        const user = await createUser({ name: 'John' });
        expect(user.name).toBe('John');
    });
});
回答模板3：选择建议
测试类型选择建议：
1.
单元测试
：核心业务逻辑、工具函数
2.
集成测试
：API接口、数据库操作
3.
端到端测试
：关键用户流程
4.
比例
：70%单元测试，20%集成测试，10%端到端测试
5.
工具
：Jest、Vitest、Cypress、Playwright
45. 如何编写可测试的代码？
中等
可测试性
代码设计
测试
查看答案
回答模板1：基础原则
编写可测试代码的基础原则：
1.
单一职责
：每个函数只做一件事
2.
依赖注入
：避免硬编码依赖
3.
纯函数
：无副作用的函数
4.
小函数
：函数保持小而专注
回答模板2：代码示例
// 不可测试的代码
function getUser(id) {
    const db = new Database(); // 硬编码依赖
    return db.query(`SELECT * FROM users WHERE id = ${id}`);
}

// 可测试的代码
function getUser(id, database) {
    return database.query(`SELECT * FROM users WHERE id = ${id}`);
}

// 纯函数
function add(a, b) {
    return a + b;
}

// 依赖注入
class UserService {
    constructor(database) {
        this.database = database;
    }
    
    getUser(id) {
        return this.database.query(`SELECT * FROM users WHERE id = ${id}`);
    }
}
回答模板3：最佳实践
可测试代码最佳实践：
1.
测试驱动开发
：先写测试再写代码
2.
模拟依赖
：使用mock模拟外部依赖
3.
测试边界
：测试边界条件
4.
测试覆盖
：保持合理的测试覆盖率
5.
重构测试
：定期重构测试代码
46. 如何进行前端性能测试？
中等
性能测试
前端测试
性能监控
查看答案
回答模板1：测试类型
前端性能测试类型：
1.
加载性能
：页面加载时间、首屏时间
2.
运行时性能
：交互响应时间、动画帧率
3.
资源性能
：文件大小、请求数量
4.
网络性能
：请求延迟、吞吐量
回答模板2：测试工具
// Lighthouse性能审计
lighthouse https://example.com --output html --output-path report.html

// WebPageTest
// 在线工具：https://www.webpagetest.org/

// 自定义性能测试
const performance = require('perf_hooks');

// 测量函数执行时间
function measurePerformance(fn) {
    const start = performance.performance.now();
    fn();
    const end = performance.performance.now();
    return end - start;
}

// 测量页面加载时间
const timing = performance.performance.timing;
const loadTime = timing.loadEventEnd - timing.navigationStart;
回答模板3：最佳实践
性能测试最佳实践：
1.
设定基准
：建立性能基准
2.
多次测试
：多次测试取平均值
3.
真实环境
：在真实环境中测试
4.
监控趋势
：监控性能变化趋势
5.
自动化
：将性能测试集成到CI/CD
🚀 CI/CD
47. 什么是CI/CD？它有哪些核心概念？
简单
CI/CD
持续集成
持续部署
查看答案
回答模板1：概念解释
CI/CD是持续集成和持续部署的缩写：
1.
CI（持续集成）
：频繁地将代码集成到共享仓库
2.
CD（持续交付）
：自动将代码交付到测试环境
3.
CD（持续部署）
：自动将代码部署到生产环境
回答模板2：流程示例
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run tests
      run: npm test
      
    - name: Build
      run: npm run build
      
    - name: Deploy
      if: github.ref == 'refs/heads/main'
      run: npm run deploy
回答模板3：最佳实践
CI/CD最佳实践：
1.
自动化
：尽可能自动化流程
2.
快速反馈
：快速提供构建结果
3.
质量门禁
：设置代码质量检查
4.
回滚机制
：准备回滚方案
5.
监控告警
：监控部署状态
48. GitHub Actions如何使用？
中等
GitHub Actions
CI/CD
自动化
查看答案
回答模板1：基础概念
GitHub Actions是GitHub的CI/CD平台，允许自动化构建、测试和部署。
核心概念：
1.
Workflow
：自动化流程
2.
Job
：工作流中的任务
3.
Step
：任务中的步骤
4.
Action
：可重用的工作单元
回答模板2：配置示例
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3
      
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build
      run: npm run build
      
    - name: Deploy to Vercel
      uses: amondnet/vercel-action@v20
      with:
        vercel-token: ${{ secrets.VERCEL_TOKEN }}
        vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
        vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
回答模板3：最佳实践
GitHub Actions最佳实践：
1.
使用缓存
：缓存依赖安装
2.
并行执行
：并行运行测试
3.
密钥管理
：使用Secrets存储敏感信息
4.
工作流重用
：使用可重用的工作流
5.
监控日志
：监控工作流执行日志
49. 如何实现蓝绿部署和金丝雀发布？
困难
蓝绿部署
金丝雀发布
部署策略
查看答案
回答模板1：蓝绿部署
蓝绿部署是同时维护两个完全相同的生产环境：
1.
蓝色环境
：当前生产环境
2.
绿色环境
：新版本环境
3.
切换
：测试通过后切换流量
4.
回滚
：快速切换回旧版本
回答模板2：金丝雀发布
金丝雀发布是逐步将流量切换到新版本：
1.
小流量
：先将少量流量切换到新版本
2.
监控
：监控新版本的表现
3.
逐步增加
：逐步增加新版本流量
4.
全量发布
：确认无问题后全量发布
# Nginx配置示例
upstream app {
    server old-version:8080 weight=90;
    server new-version:8080 weight=10;
}
回答模板3：选择建议
部署策略选择建议：
1.
蓝绿部署
：需要快速回滚、停机时间短
2.
金丝雀发布
：需要逐步验证、降低风险
3.
滚动更新
：资源有限、逐步更新
4.
A/B测试
：需要功能对比
5.
考虑因素
：团队能力、基础设施、业务需求
50. 如何监控和告警CI/CD流程？
中等
监控
告警
CI/CD
查看答案
回答模板1：监控指标
CI/CD监控指标：
1.
构建状态
：构建成功/失败
2.
构建时间
：构建耗时
3.
测试覆盖率
：代码测试覆盖率
4.
部署频率
：部署次数
回答模板2：告警配置
# GitHub Actions告警
name: Alert on Failure

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  alert:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    
    steps:
    - name: Send Slack Notification
      uses: 8398a7/action-slack@v3
      with:
        status: failure
        channel: '#alerts'
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
回答模板3：最佳实践
CI/CD监控最佳实践：
1.
实时监控
：实时监控构建状态
2.
多渠道告警
：邮件、Slack、钉钉等
3.
历史分析
：分析构建历史趋势
4.
优化建议
：基于数据优化流程
5.
定期审计
：定期审计CI/CD流程
51. 如何实现前端项目的自动化测试？
中等
自动化测试
测试框架
CI/CD集成
查看答案
回答模板1：测试框架
前端自动化测试框架：
1.
Jest
：Facebook的测试框架
2.
Vitest
：Vite的测试框架
3.
Cypress
：端到端测试框架
4.
Playwright
：跨浏览器测试框架
回答模板2：配置示例
// Jest配置
// jest.config.js
module.exports = {
    testEnvironment: 'jsdom',
    setupFilesAfterEnv: ['
/src/setupTests.js'],
    coverageThreshold: {
        global: {
            branches: 80,
            functions: 80,
            lines: 80
        }
    }
};

// package.json
{
    "scripts": {
        "test": "jest",
        "test:coverage": "jest --coverage",
        "test:watch": "jest --watch"
    }
}
回答模板3：最佳实践
自动化测试最佳实践：
1.
测试金字塔
：遵循测试金字塔原则
2.
CI集成
：将测试集成到CI/CD
3.
代码覆盖率
：监控测试覆盖率
4.
并行测试
：并行运行测试
5.
测试报告
：生成测试报告
📚 工程化与工具链面试题库 - 45道精选题目
持续更新中... | 支持移动端访问