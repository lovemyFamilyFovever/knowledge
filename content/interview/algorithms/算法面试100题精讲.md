---
title: "算法面试100题精讲"
tags: []
source: "baike"
source_path: "技术文章 / 编程语言"
collected: "2026-09-05"
status: "imported"
---

# 算法面试100题精讲

# 算法面试100题精讲

## 1) 数组与哈希（15题）

### 题目1：两数之和（LeetCode 1）
**题目描述：**
给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出和为目标值的那两个整数，并返回它们的数组下标。你可以假设每种输入只会对应一个答案。但是，数组中同一个元素在答案里不能重复出现。你可以按任意顺序返回答案。

**思路分析：**
1. **暴力解法**：双重循环遍历所有可能的数对，时间复杂度O(n²)
2. **哈希表优化**：一次遍历，用哈希表存储已遍历的数及其下标，检查 `target-nums[i]` 是否存在
3. **最优解**：哈希表查找时间复杂度O(1)，总时间复杂度O(n)

**代码实现：**
```python
# Python解法
def twoSum(nums, target):
    hash_map = {}
    for i, num in enumerate(nums):
        complement = target - num
        if complement in hash_map:
            return [hash_map[complement], i]
        hash_map[num] = i
    return []
```

```go
// Go解法
func twoSum(nums []int, target int) []int {
    hashMap := make(map[int]int)
    for i, num := range nums {
        complement := target - num
        if j, ok := hashMap[complement]; ok {
            return []int{j, i}
        }
        hashMap[num] = i
    }
    return nil
}
```

**复杂度分析：**
- 时间复杂度：O(n)，单次遍历
- 空间复杂度：O(n)，哈希表存储

**同类题目推荐：**
1. 三数之和（LeetCode 15）
2. 四数之和（LeetCode 18）
3. 有效的字母异位词（LeetCode 242）
4. 字母异位词分组（LeetCode 49）
5. 最长连续序列（LeetCode 128）

### 题目2：有效的字母异位词（LeetCode 242）
**题目描述：**
给定两个字符串 `s` 和 `t`，编写一个函数来判断 `t` 是否是 `s` 的字母异位词。注意：若 `s` 和 `t` 中每个字符出现的次数都相同，则称 `s` 和 `t` 互为字母异位词。

**思路分析：**
1. **排序法**：将两个字符串排序后比较，时间复杂度O(n log n)
2. **哈希表法**：统计字符出现频率，比较两个哈希表是否相等
3. **数组计数法**：使用长度为26的数组统计字符频率（针对小写字母）

**代码实现：**
```python
# Python解法
def isAnagram(s, t):
    if len(s) != len(t):
        return False
    
    # 方法1：哈希表
    count = {}
    for char in s:
        count[char] = count.get(char, 0) + 1
    
    for char in t:
        if char not in count or count[char] == 0:
            return False
        count[char] -= 1
    
    return True

# 方法2：计数器
from collections import Counter
def isAnagram2(s, t):
    return Counter(s) == Counter(t)
```

```go
// Go解法
func isAnagram(s string, t string) bool {
    if len(s) != len(t) {
        return false
    }
    
    count := make(map[rune]int)
    for _, ch := range s {
        count[ch]++
    }
    
    for _, ch := range t {
        count[ch]--
        if count[ch] < 0 {
            return false
        }
    }
    
    return true
}
```

**复杂度分析：**
- 时间复杂度：O(n)
- 空间复杂度：O(k)，k为字符集大小

**同类题目推荐：**
1. 赎金信（LeetCode 383）
2. 字符串中的第一个唯一字符（LeetCode 387）
3. 找到字符串中所有字母异位词（LeetCode 438）
4. 最长回文串（LeetCode 409）

## 2) 双指针（10题）

### 题目1：盛最多水的容器（LeetCode 11）
**题目描述：**
给你 `n` 个非负整数 `a1, a2, ..., an`，每个数代表坐标中的一个点 `(i, ai)`。在坐标内画 `n` 条垂直线，垂直线 `i` 的两个端点分别为 `(i, ai)` 和 `(i, 0)`。找出其中的两条线，使得它们与 `x` 轴共同构成的容器可以容纳最多的水。不能倾斜容器。

**思路分析：**
1. **暴力解法**：双重循环计算所有组合，时间复杂度O(n²)
2. **双指针法**：从两端向中间移动，每次移动高度较小的指针
3. **贪心策略**：移动高度较大的指针不可能获得更大面积

**代码实现：**
```python
# Python解法
def maxArea(height):
    left, right = 0, len(height) - 1
    max_area = 0
    
    while left < right:
        # 计算当前面积
        width = right - left
        h = min(height[left], height[right])
        current_area = width * h
        max_area = max(max_area, current_area)
        
        # 移动高度较小的指针
        if height[left] < height[right]:
            left += 1
        else:
            right -= 1
    
    return max_area
```

```go
// Go解法
func maxArea(height []int) int {
    left, right := 0, len(height)-1
    maxArea := 0
    
    for left < right {
        width := right - left
        h := min(height[left], height[right])
        currentArea := width * h
        maxArea = max(maxArea, currentArea)
        
        if height[left] < height[right] {
            left++
        } else {
            right--
        }
    }
    
    return maxArea
}

func min(a, b int) int {
    if a < b {
        return a
    }
    return b
}

func max(a, b int) int {
    if a > b {
        return a
    }
    return b
}
```

**复杂度分析：**
- 时间复杂度：O(n)，双指针最多移动n次
- 空间复杂度：O(1)

**同类题目推荐：**
1. 接雨水（LeetCode 42）
2. 三数之和（LeetCode 15）
3. 最接近的三数之和（LeetCode 16）
4. 移除元素（LeetCode 27）

### 题目2：三数之和（LeetCode 15）
**题目描述：**
给你一个包含 `n` 个整数的数组 `nums`，判断 `nums` 中是否存在三个元素 `a, b, c`，使得 `a + b + c = 0`？请你找出所有和为 `0` 且不重复的三元组。

**思路分析：**
1. **排序**：先对数组排序，便于去重和双指针操作
2. **双指针**：固定第一个数，用双指针找另外两个数
3. **去重**：跳过重复元素，避免重复三元组

**代码实现：**
```python
# Python解法
def threeSum(nums):
    nums.sort()
    result = []
    n = len(nums)
    
    for i in range(n - 2):
        # 跳过重复的第一个数
        if i > 0 and nums[i] == nums[i - 1]:
            continue
        
        # 双指针
        left, right = i + 1, n - 1
        while left < right:
            total = nums[i] + nums[left] + nums[right]
            
            if total < 0:
                left += 1
            elif total > 0:
                right -= 1
            else:
                result.append([nums[i], nums[left], nums[right]])
                # 跳过重复的第二个和第三个数
                while left < right and nums[left] == nums[left + 1]:
                    left += 1
                while left < right and nums[right] == nums[right - 1]:
                    right -= 1
                left += 1
                right -= 1
    
    return result
```

```go
// Go解法
func threeSum(nums []int) [][]int {
    sort.Ints(nums)
    result := [][]int{}
    n := len(nums)
    
    for i := 0; i < n-2; i++ {
        // 跳过重复的第一个数
        if i > 0 && nums[i] == nums[i-1] {
            continue
        }
        
        // 双指针
        left, right := i+1, n-1
        for left < right {
            total := nums[i] + nums[left] + nums[right]
            
            if total < 0 {
                left++
            } else if total > 0 {
                right--
            } else {
                result = append(result, []int{nums[i], nums[left], nums[right]})
                // 跳过重复的第二个和第三个数
                for left < right && nums[left] == nums[left+1] {
                    left++
                }
                for left < right && nums[right] == nums[right-1] {
                    right--
                }
                left++
                right--
            }
        }
    }
    
    return result
}
```

**复杂度分析：**
- 时间复杂度：O(n²)，排序O(n log n) + 双重循环O(n²)
- 空间复杂度：O(log n)到O(n)，取决于排序算法

## 3) 滑动窗口（8题）

### 题目1：无重复字符的最长子串（LeetCode 3）
**题目描述：**
给定一个字符串，请你找出其中不含有重复字符的 **最长子串** 的长度。

**思路分析：**
1. **暴力解法**：检查所有子串，时间复杂度O(n³)
2. **滑动窗口**：维护一个没有重复字符的窗口，记录最大长度
3. **哈希表优化**：记录字符最后出现的位置，快速移动左边界

**代码实现：**
```python
# Python解法
def lengthOfLongestSubstring(s):
    char_index = {}
    left = 0
    max_length = 0
    
    for right, char in enumerate(s):
        # 如果字符已出现且位置在窗口内，移动左边界
        if char in char_index and char_index[char] >= left:
            left = char_index[char] + 1
        
        # 更新字符位置
        char_index[char] = right
        # 更新最大长度
        max_length = max(max_length, right - left + 1)
    
    return max_length
```

```go
// Go解法
func lengthOfLongestSubstring(s string) int {
    charIndex := make(map[byte]int)
    left, maxLength := 0, 0
    
    for right := 0; right < len(s); right++ {
        char := s[right]
        // 如果字符已出现且位置在窗口内，移动左边界
        if idx, ok := charIndex[char]; ok && idx >= left {
            left = idx + 1
        }
        
        // 更新字符位置
        charIndex[char] = right
        // 更新最大长度
        if right-left+1 > maxLength {
            maxLength = right - left + 1
        }
    }
    
    return maxLength
}
```

**复杂度分析：**
- 时间复杂度：O(n)，一次遍历
- 空间复杂度：O(min(m, n))，m为字符集大小

**同类题目推荐：**
1. 最小覆盖子串（LeetCode 76）
2. 串联所有单词的子串（LeetCode 30）
3. 长度最小的子数组（LeetCode 209）
4. 滑动窗口最大值（LeetCode 239）

### 题目2：找到字符串中所有字母异位词（LeetCode 438）
**题目描述：**
给定两个字符串 `s` 和 `p`，找到 `s` 中所有 `p` 的异位词的子串，返回这些子串的起始索引。串 `s` 和 `p` 中只包含小写英文字母。

**思路分析：**
1. **滑动窗口**：维护一个长度为p.length()的窗口
2. **频率计数**：比较窗口内字符频率与p的频率
3. **优化**：使用计数器记录需要匹配的字符数

**代码实现：**
```python
# Python解法
from collections import Counter

def findAnagrams(s, p):
    if len(s) < len(p):
        return []
    
    p_count = Counter(p)
    s_count = Counter()
    result = []
    
    left = 0
    for right in range(len(s)):
        # 扩展右边界
        s_count[s[right]] += 1
        
        # 当窗口大小等于p长度时
        if right - left + 1 == len(p):
            # 检查是否为异位词
            if s_count == p_count:
                result.append(left)
            
            # 收缩左边界
            s_count[s[left]] -= 1
            if s_count[s[left]] == 0:
                del s_count[s[left]]
            left += 1
    
    return result
```

```go
// Go解法
func findAnagrams(s string, p string) []int {
    if len(s) < len(p) {
        return nil
    }
    
    pCount := make(map[byte]int)
    for i := 0; i < len(p); i++ {
        pCount[p[i]]++
    }
    
    sCount := make(map[byte]int)
    result := []int{}
    left := 0
    
    for right := 0; right < len(s); right++ {
        // 扩展右边界
        sCount[s[right]]++
        
        // 当窗口大小等于p长度时
        if right-left+1 == len(p) {
            // 检查是否为异位词
            if mapsEqual(sCount, pCount) {
                result = append(result, left)
            }
            
            // 收缩左边界
            sCount[s[left]]--
            if sCount[s[left]] == 0 {
                delete(sCount, s[left])
            }
            left++
        }
    }
    
    return result
}

func mapsEqual(a, b map[byte]int) bool {
    if len(a) != len(b) {
        return false
    }
    for k, v := range a {
        if b[k] != v {
            return false
        }
    }
    return true
}
```

**复杂度分析：**
- 时间复杂度：O(n + m)，n为s长度，m为p长度
- 空间复杂度：O(k)，k为字符集大小

## 4) 栈与队列（8题）

### 题目1：有效的括号（LeetCode 20）
**题目描述：**
给定一个只包括 `'('`，`')'`，`'{'`，`'}'`，`'['`，`']'` 的字符串，判断字符串是否有效。有效字符串需满足：
1. 左括号必须用相同类型的右括号闭合
2. 左括号必须以正确的顺序闭合
3. 每个右括号都有一个对应的相同类型的左括号

**思路分析：**
1. **栈的应用**：遇到左括号入栈，遇到右括号出栈匹配
2. **映射关系**：使用哈希表存储括号对应关系
3. **边界处理**：栈为空时遇到右括号无效

**代码实现：**
```python
# Python解法
def isValid(s):
    stack = []
    mapping = {')': '(', ']': '[', '}': '{'}
    
    for char in s:
        if char in mapping.values():
            stack.append(char)
        elif char in mapping:
            if not stack or stack.pop() != mapping[char]:
                return False
        else:
            return False
    
    return len(stack) == 0
```

```go
// Go解法
func isValid(s string) bool {
    stack := []rune{}
    mapping := map[rune]rune{')': '(', ']': '[', '}': '{'}
    
    for _, char := range s {
        if char == '(' || char == '[' || char == '{' {
            stack = append(stack, char)
        } else if char == ')' || char == ']' || char == '}' {
            if len(stack) == 0 || stack[len(stack)-1] != mapping[char] {
                return false
            }
            stack = stack[:len(stack)-1]
        }
    }
    
    return len(stack) == 0
}
```

**复杂度分析：**
- 时间复杂度：O(n)
- 空间复杂度：O(n)，最坏情况全是左括号

**同类题目推荐：**
1. 最小栈（LeetCode 155）
2. 用栈实现队列（LeetCode 232）
3. 每日温度（LeetCode 739）
4. 基本计算器（LeetCode 224）

### 题目2：柱状图中最大的矩形（LeetCode 84）
**题目描述：**
给定 `n` 个非负整数，用来表示柱状图中各个柱子的高度。每个柱子彼此相邻，且宽度为 1。求在该柱状图中，能够勾勒出来的矩形的最大面积。

**思路分析：**