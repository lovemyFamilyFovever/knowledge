# 09 — 开发实施提示词（多 Agent 并行开发）

> 用途：AI 金采集标注系统 MVP（明早演示黄金路径）在 **code 项目 ERP 后台**落地。本文档拆分为 8 个 Agent 任务包，文件所有权严格隔离，可并行开发。**每个 Agent 只能改自己名下的文件，禁止触碰其他 Agent 的文件。**

## 修订记录

| 版本 | 日期 | 修订说明 |
|---|---|---|
| V1.0 | 2026-08 | 初版 |
| V1.1 | 2026-08-20 | P0 修复：①启动命令改为 `dev:server` / `dev:client`（可 `dev:local` 一键起）；②BailianService 实际签名更正（`askFastSync(question, history, sessionId)` / `askSync(question, sessionId?)`）并给出推荐调用样板；③凡用 ErpAuthGuard 的模块 providers 必须同时含 `ErpAuthGuard` + `ErpAuthService`；④新增 Agent 1 的 SQL 手工执行职责与交付门槛（项目无自动迁移）；⑤@antv/g6 v5 已由人工安装，删除 npm pkg set 与 CDN 兜底表述。P1 修复：CSRF 全集（所有请求含 GET）、ERP 前端栈明示、抽取 prompt 硬 checklist、LLM 返回 JSON 运行时校验与重试策略、路由阈值载体常量、金标准验收项移交人工、交付说明补飞书上线警告。新增：代码隔离于 `erp/data-collect/`、里程碑 git 提交规范、开发日志汇总、mock 兜底原则、部署禁令（禁 miaoda deploy / 禁碰 online 库）。顺带修复：红线 1 与 G6 安装表述的矛盾、Agent 4 finalize 补 editing 中间态、注释红线可判据化、Agent 8 补 bug 分级判据、0.1 注明 08 文档 SQL 编号表述已过时（实际从 035 起）。 |

---

# 第 0 部分：公共上下文（所有 Agent 必读，先读这部分再开工）

## 0.1 项目位置与文档

- 项目根目录：`C:\Users\liuxi\Desktop\code`
- 方案文档（必读，按需引用）：
  - `C:\Users\liuxi\Desktop\AI金采集标注系统方案\02-数据模型设计.md`（表结构权威）
  - `C:\Users\liuxi\Desktop\AI金采集标注系统方案\03-提取与校核流程.md`（流程）
  - `C:\Users\liuxi\Desktop\AI金采集标注系统方案\04-知识图谱设计.md`（图谱）
  - `C:\Users\liuxi\Desktop\AI金采集标注系统方案\05-输出引擎设计.md`（输出）
  - `C:\Users\liuxi\Desktop\AI金采集标注系统方案\08-MVP技术落地与接口契约.md`（**API/JSON 契约权威，本文档内联了关键契约，两处冲突以本文档为准**）
- 项目规范：`C:\Users\liuxi\Desktop\code\AGENTS.md`（必读，硬性约束）

> ⚠️ 勘误：08 文档中「SQL 迁移编号从 034 起」的表述已过时——`034-create-feishu-user-bindings.sql` 已被占用，**实际新脚本从 035 起**，以本 09 文档 §0.3 为准。<!-- 2026-08-23 更新 -->**当前上限已到 045（含 041 词典管理 4 表、045 文章多格式输入 2 表），新脚本从 046 起；046 起新表禁用 `dict_` 等历史前缀，须用合规前缀（见 AGENTS.md）。**

## 0.2 技术栈与运行方式

- 后端：NestJS 10（TypeScript），`server/`；前端：Vue 3 + TS + Tailwind v4，`client/`；ERP 页面独立在 `erp/` 目录
- ERP 鉴权：`erp_auth_token`（localStorage），请求头 `Authorization: Bearer <token>`；后端用 `ErpAuthGuard`（参考 `server/modules/xiuma-sync/xiuma-sync.module.ts` 的写法）。**凡使用 ErpAuthGuard 的模块，providers 必须同时包含 `ErpAuthGuard` 与 `ErpAuthService`（两者均 import 自 `erp/server/`），参照 `server/modules/xiuma-sync/xiuma-sync.module.ts` 写法，缺任一个 NestJS 启动即崩溃**
- ERP 前端页面结构：每个功能一个目录 `erp/<功能名>/index.vue`，路由注册在 `client/src/router/index.ts`（ErpLayout 子路由，参考现有 erp 路由写法），侧边栏菜单在 `erp/server/erp-sidebar.service.ts` 的 `MENU_ITEMS`（`erp/ErpLayout.vue` 消费）
- 前端 API 封装模式：参考 `erp/xiuma-data-import/`（含 `getApiBase()` + `getAuthHeaders()`）。**CSRF 全集：飞书环境所有请求（含 GET）必须携带 `x-suda-csrf-token` 请求头，token 取自 `document.cookie` 中的 `suda-csrf-token`**（取值函数见 `client/src/api/ai.ts` 的 `getCsrfToken`）。⚠️ 参考页 `erp/xiuma-data-import/index.vue` 的 fetch 未带该 header，属事故同款写法，**不要照抄这一点**；正确范例见 `client/src/api/trade.ts` 的 `tradeGet`
- LLM 调用：**复用现有 `BailianService`**（`server/modules/ai/bailian.service.ts`，已在 AiModule exports，通过 `imports: [AiModule]` 引入）。**实际方法签名（已核实）**：
  - `askFastSync(question: string, history: Array<{ role: 'user' | 'assistant'; content: string }>, sessionId = 'default')`
  - `askSync(question: string, sessionId?: string)`（同步返回 `{ text, ... }`）
  - **推荐调用样板**（抽取/生成类单轮任务）：`bailianService.askSync(prompt, 'collect-extract')`——传固定 sessionId 隔离会话；生成内容场景用 `askSync(prompt, 'content-gen')`
- 数据库：PostgreSQL + Drizzle ORM；统一 `@Inject(DRIZZLE_DB)` 注入（见 `server/database/database.module.ts`）；Repository 模式参考 `server/database/xiuma-product-snapshot.repository.ts`。**项目无自动迁移机制，SQL 文件需手工执行（见 Agent 1 任务）**
- 校验：DTO 用 interface（禁止 class-validator）
- 命令：`npm run type:check`（提交前必须通过）；后端 `npm run dev:server`；前端 `npm run dev:client`；也可 `npm run dev:local` 一键起前后端（**package.json 只有这三个 dev 脚本，不存在 `dev:server:win` / `dev:client:win`**）

## 0.3 数据库硬性规范（所有涉及建表/Repository 的 Agent 遵守）

1. **SQL 迁移文件放 `server/database/sql/`，编号从 035 起**（034 已被占用：034-create-feishu-user-bindings.sql）
2. 每表独立迁移文件：`035-create-collect-dict-tables.sql`、`036-create-data-articles.sql`、`037-create-data-events.sql`、`038-create-data-daily-price.sql`、`039-create-data-content-tables.sql`、`040-create-kg-tables.sql`（编号可自行微调，但必须 > 034 且不重号）
3. 必须幂等：`CREATE TABLE IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS`
4. 每表必含 4 审计列：`_created_at TIMESTAMPTZ DEFAULT now()`、`_updated_at TIMESTAMPTZ DEFAULT now()`、`_created_by TEXT`、`_updated_by TEXT`
5. 主键 `UUID PRIMARY KEY DEFAULT gen_random_uuid()`（若妙搭环境无此函数，改用应用层生成）
6. 每表 `COMMENT ON TABLE`、每列 `COMMENT ON COLUMN`；枚举字段注释写明取值范围
7. `server/database/schema.ts` **手工合并**新增表定义（禁止从妙搭下载覆盖；按现有 schema.ts 风格追加 pgTable 定义）
8. 表前缀：`dict_`（字典）/ `data_`（业务数据）/ `kg_`（图谱）

## 0.4 硬性红线（所有 Agent）

1. **禁止改 `package.json` 依赖（@antv/g6 已由人工安装）**；同时禁止修改 `vite.config.ts` / `nest-cli.json` / `tsconfig*`（如确需其他新依赖，仅在最终交付说明中列出，由人工统一处理）
2. 禁止修改 `server/modules/settlement/`（同事模块，只审查不修改）
3. 禁止改动 `server/app.module.ts`（由 Agent 8 统一注册）
4. 禁止修改其他 Agent 名下文件
5. 注释仅保留方法级一行摘要与必要的业务规则说明（可判据：无方法摘要、无业务规则依据的注释一律不加）
6. **git 提交规范**：每个 Agent 完成一个里程碑后执行 `git commit`；只 add 自己本次修改的文件，禁止混入无关文件；提交信息用中文详细日志（改动内容 + 原因），便于回滚与查看
7. 结算模块 `docs/结算系统` 相关文件不动
8. **部署禁令**：全程严禁执行 `miaoda deploy`、严禁触碰 online 库（online 库 033 迁移冻结期，误部署会导致改名表接口 42P01）

## 0.5 明早演示黄金路径（所有 Agent 的目标一致）

```
真实文章 → AI 自动识别 → AI 自动结构化 → 人工修改 1~3 个字段 → 确认入库
  → 自动生成实体/关系 → 图谱看到关系 → 一键生成内容草稿（公众号/51APP/行业群 3 个成品）
```

## 0.6 代码隔离与协作通则（所有 Agent）

1. **代码隔离**：所有新代码集中在 `erp/data-collect/` 一个文件夹内——前端页面为 `erp/data-collect/<页面名>/index.vue`，后端 module/controller/service 全部放 `erp/data-collect/server/`（各模块再按 collect / kg / content 子目录区分所有权）。例外清单：
   - Repository 放 `server/database/`（可复用数据层）
   - SQL 迁移放 `server/database/sql/`
   - 集成点单行改动：`server/app.module.ts` 模块注册、`client/src/router/index.ts` 路由注册、菜单注册（`erp/server/erp-sidebar.service.ts` 的 `MENU_ITEMS`，由 `erp/ErpLayout.vue` 消费）——**三处均由 Agent 8 统一执行**
2. API 路径仍按 08 契约：`/api/collect/*`、`/api/kg/*`、`/api/content/*`（与文件物理位置无关）
3. **开发日志**：全程记录踩坑、困难与自行决策点；各 Agent 在完成报告中列出，由 Agent 8 汇总写入 `docs/AI采集标注/开发日志.md`
4. **兜底原则**：中途卡住先自行决断；无法决断时用 mock 数据兜底保证流程顺畅，但必须在完成报告中记录何处 mock、为何 mock
5. **部署禁令**（同红线 8）：严禁 `miaoda deploy`、严禁触碰 online 库；本次只在本地库（192.168.1.64/bxg_app）开发验证

---

# 第 1 部分：Agent 任务包

---

## Agent 1 — 数据库地基（最先启动，其他 Agent 联调依赖它）

**角色**：建表 + schema.ts + 三个 Repository + **SQL 在本地库执行**。

**文件所有权（独占）**：
- `server/database/sql/035-*.sql` ~ `040-*.sql`（新建）
- `server/database/schema.ts`（只追加本次表定义）
- `server/database/collect.repository.ts`（新建）
- `server/database/kg.repository.ts`（新建）
- `server/database/content.repository.ts`（新建）

**禁止触碰**：其他 Agent 文件、app.module.ts。

**任务**：
1. 按 02 文档建全部表（MVP 范围）：
   - 字典表：dict_industry_category、dict_grade、dict_region、dict_event_category、dict_event_sub_category、dict_inventory_type、dict_tax_status、dict_analysis_type、dict_forecast_model、dict_channel、dict_article_style、dict_entity_alias（含字段结构：alias/standard_name/entity_type/source/is_confirmed）
   - data_articles（含去重键 content_hash+source_name 复合唯一、status、ai_draft、duplicate_of）
   - data_events（E1-E11 + O 字段 + validity_days/valid_until + source_article_id + status 含 skipped/rejected + ai_draft）
   - data_daily_price（唯一键含 price_date/category/grade/brand_short_name/origin/region/market/spec/tax_status/unit + status active/draft/archived + source_article_id）
   - data_weekly_inventory / data_monthly_production / data_quarterly_demand（仅建表，字段按 02 文档）
   - data_analysis / data_forecast（仅建表，按 02 文档：forecast 含 review_status + verification_status + forecast_error_pct + historical_accuracy_pct）
   - data_content（生成内容：content_id/title/content/content_type/channel/style/version/source_record_ids/citations/generation_history/reviewer/status/source_article_id）+ data_content_citations（content_id/record_type/record_id/position/evidence）
   - kg_entities（entity_id/name/node_category[entity|record]/aliases/entity_type/biz_record_type/biz_record_id/properties/description/confidence/source_ids/is_verified/status + UNIQUE(biz_record_type,biz_record_id)）
   - kg_relations（relation_id/source_entity_id/target_entity_id/relation_type/nature[FACT|OPINION|INFERENCE]/properties/confidence/source_ids/is_verified/status）
2. 字典数据初始化 SQL（7 大类事件枚举、区域、品类、钢种、渠道、风格、库存类型、含税、解读类型、预测模型）
3. schema.ts 手工合并全部新表
4. **执行 SQL（明确职责：项目无自动迁移机制）**：写完 035~040 后，用 `.env` 中 `DATABASE_URL`（192.168.1.64/bxg_app）通过 node 脚本（可复用项目 pg/Drizzle 连接）或 psql **逐个执行**；每个脚本执行前先 `SELECT COUNT(*)` 探测目标表（确认表不存在或可幂等重入），执行后 COUNT 校验种子数据行数；执行与校验结果写入交付说明
5. 实现三个 Repository（方法签名如下，**与后端 Agent 2/3/4 的调用契约**）：

```typescript
// collect.repository.ts —— Agent 2 使用
createArticle(data): Promise<ArticleRecord>                       // 插入文章（自动算 content_hash）
findArticleByHashAndSource(hash, sourceName)                      // 去重查询
findArticleById(id)
updateArticleStatus(id, status, extra?)
createSubRecord(table: 'events'|'daily_price'|..., data)          // 创建 draft 子记录（写入对应表）
updateSubRecord(table, id, data)
findSubRecordsByArticleId(table, articleId)
findPendingWorkbench(): Promise<WorkbenchTodo[]>                  // 工作台待处理队列
findRecordWithDraft(table, id)                                    // 子记录 + ai_draft

// kg.repository.ts —— Agent 2 调用（图谱写入）、Agent 3 调用（查询）
createEntity(data): Promise<EntityRecord>                         // MERGE 语义：按 name+entity_type 查重，命中补 source_ids/aliases
createRelation(data): Promise<RelationRecord>                     // relation_id 唯一，同对节点同类型允许多条
searchEntities(keyword, limit?)                                   // 名称/别名模糊搜索
getSubgraph(entityId, depth, relationTypes?)                      // 递归查询，返回 {nodes, edges}
getEntityById(entityId)
getEntityByBizRecord(recordType, recordId)                        // 业务记录节点定位
getGraphStats(): Promise<{nodes, edges, byType}>

// content.repository.ts —— Agent 4 使用
createContent(data): Promise<ContentRecord>
updateContent(id, data)
getContentById(id)
listContents(filter?)
createCitation(data)
listCitationsByContent(contentId)
listConfirmedMaterials(materialTypes): Promise<MaterialRecord[]>  // 只查 confirmed/active 素材（素材池）
```

**验收**：`npm run type:check` 通过；SQL 文件幂等可重复执行；三 Repository 方法签名与上方契约一致；**035~040 已在本地库执行成功且 COUNT 校验通过（交付门槛，见协作速查）**。

---

## Agent 2 — 采集后端模块（erp/data-collect/server/collect/）

**角色**：文章提交/去重/类型路由/约束抽取/校核/跳过/放弃/重试/工作台/AI 修改建议 API。

**文件所有权（独占）**：`erp/data-collect/server/collect/` 整个目录（controller/service/module/dto 均新建）。
**禁止触碰**：app.module.ts（Agent 8 注册）、database 目录（只 import Agent 1 的 Repository）。

**任务**：
1. 新建 `CollectModule`（imports: [AiModule]（用 BailianService）、providers: [CollectService, CollectRepository, KgRepository, **ErpAuthService, ErpAuthGuard**（import 自 `erp/server/`，缺任一个 NestJS 启动即崩，参照 `server/modules/xiuma-sync/xiuma-sync.module.ts`）]）
2. 实现 API（前缀 `@Controller('api/collect')`，鉴权参考 xiuma-sync：`@Public()` + `@UseGuards(ErpAuthGuard)`）：
   - `POST /api/collect/articles`（提交文章：title/content/source_url/source_name；服务端算 content_hash，按 content_hash+source_name 去重，重复返回 duplicate）
   - `POST /api/collect/articles/:id/route`（AI 类型路由：调 `bailianService.askSync(prompt, 'collect-extract')`（签名见 §0.2），prompt 按 08 文档 4.1，返回 {route, confidence, mixed_components, mvp_supported}；**路由全集 9 类可识别，但 MVP 只处理 event/data_price/mixed（mixed 仅限 event+data_price 组合），其余类型返回"模板未启用"提示**；**阈值写死为 CollectService 内常量 `ROUTE_CONFIDENCE_THRESHOLD = 0.8`**）
   - `POST /api/collect/articles/:id/extract`（约束抽取：按路由结果加载事件/日价模板（08 文档 4.2/4.3 的 JSON Schema），调 `bailianService.askSync(prompt, 'collect-extract')`，解析并校验 JSON，**每个字段带 confidence/nature/evidence 写入 ai_draft**，创建 draft 子记录；mixed 创建多条子记录）
     - **抽取 prompt 硬 checklist**（08 只给了 JSON Schema 示例、没给 prompt 文本，prompt 必须满足以下全部）：① 角色设定（不锈钢行业信息抽取专家）；② 目标 JSON Schema 原文（完整贴入，不得省略字段）；③ 枚举值清单（引用 Agent 1 字典表取值）；④ 硬性规则「无证据字段必须返回 null，不得编造」；⑤ 每字段输出 `confidence`(high/mid/low)、`nature`(FACT/OPINION/INFERENCE)、`evidence`（原文引用片段）。联调前必须用固定测试文章做抽取冒烟，结果附在完成报告
     - **LLM 返回 JSON 运行时校验方案**：`JSON.parse` + 手写结构校验函数（必填字段/枚举取值/数值类型逐项检查）；校验失败按 08 §8 错误码触发重试（指数退避 1s/2s/4s，最多重试 2 次）；仍失败标 `failed`，走手动填表兜底（对应 /retry 接口）
   - `POST /api/collect/records/:id/review`（校核提交：body 含 {fields: {...}, field_reviewed: {...}}；执行硬拦截校验（低置信度未确认/必填空/判断类未确认/INFERENCE 未确认）；通过则子记录 confirmed（数据轨 active）+ 更新父任务聚合状态 + **调用 KgRepository 写入图谱**（事件→节点+关系；日价→节点+IMPACTS 边））
   - `POST /api/collect/records/:id/skip`（状态 skipped 回队列）
   - `POST /api/collect/records/:id/reject`（状态 rejected，不写图谱）
   - `POST /api/collect/records/:id/retry`（failed → 重新抽取；或手动填表 draft）
   - `GET /api/collect/workbench/todos`（待处理队列：待校核/失败重试/已跳过，含数量统计）
   - `GET /api/collect/articles/:id`（文章 + 子记录列表 + 各子记录 ai_draft 详情）
   - `POST /api/collect/records/:id/ai-suggestion`（AI 修改建议：MVP 先做"历史同类值"规则——按同 category/sub_category 查最近 5 条已确认记录的平均/众数作为建议值）
3. 父任务聚合函数（02 文档 3.1：全 confirmed→completed；全 draft→reviewing；部分→partial 等）在 Service 内实现，每次子记录状态变更后重算

**契约要点**：
- 审核判定组合规则：判断类字段（E10/E11/I1-I4/A13/B13/P5/Q5/R5/P12）即使高置信度也硬拦截必须人工
- INFERENCE 性质字段未确认 → 硬拦截
- 数据轨（日价）AI 抽取后直接 active，仅低置信度/异常转 draft
- 响应格式：`{ code: 0, data, msg }`（沿用项目风格）

**验收**：`npm run type:check` 通过；curl 可跑通"提交→路由→抽取→review 确认→图谱节点出现"链路（可先用测试文章）。

---

## Agent 3 — 图谱查询模块（erp/data-collect/server/kg/）

**角色**：图谱查询 API（搜索/子图/详情/统计）。

**文件所有权（独占）**：`erp/data-collect/server/kg/` 整个目录。
**禁止触碰**：app.module.ts、database 目录（import Agent 1 的 KgRepository）、collect 模块。

**任务**：
1. 新建 `KgModule`（providers: [KgService, KgRepository, **ErpAuthService, ErpAuthGuard**（import 自 `erp/server/`，参照 xiuma-sync.module.ts）]）
2. 实现 API（前缀 `@Controller('api/kg')`，`@Public()` + `@UseGuards(ErpAuthGuard)`）：
   - `GET /api/kg/search?q=太钢`（实体搜索：名称/别名 LIKE；返回 [{entity_id, name, node_category, entity_type}]）
   - `GET /api/kg/subgraph?entityId=&depth=2&types=IMPACTS,CAUSED_BY`（递归查询，**depth 限制 ≤6 默认 2，带环路检测**；返回 {nodes, edges}，节点含 category/type，边含 relation_type/nature；同类型多边按 properties.time 倒序）
   - `GET /api/kg/entity/:id`（详情：属性 + aliases + source_ids + 关联边数）
   - `GET /api/kg/stats`（节点总数/关系总数/按 node_category 与 entity_type 分布）
3. G6 渲染输入格式（08 文档 6.3）：nodes [{id,label,category,type}]、edges [{id,source,target,label,nature}]

**验收**：`npm run type:check` 通过；用 Agent 1 造 2-3 条测试数据后可查到子图。

---

## Agent 4 — 内容生成模块（erp/data-collect/server/content/）

**角色**：素材包聚合 + 内容生成/重生成/定稿/引用标注。

**文件所有权（独占）**：`erp/data-collect/server/content/` 整个目录。
**禁止触碰**：app.module.ts、database 目录（import Agent 1 的 ContentRepository/KgRepository）、collect 模块。

**任务**：
1. 新建 `ContentModule`（imports: [AiModule]，providers 含 **ErpAuthService, ErpAuthGuard**（import 自 `erp/server/`，参照 xiuma-sync.module.ts））
2. 实现 API（前缀 `@Controller('api/content')`，`@Public()` + `@UseGuards(ErpAuthGuard)`）：
   - `POST /api/content/generate`（body：{record_ids: [...], channel, style}；**只允许 confirmed/active 素材**（draft/archived 拒绝，错误码 E5001）；聚合素材包（08 文档 7.1：core_fact/time/subject/grades/impact/prices/analysis/forecast/sources）；按渠道×风格兼容矩阵校验（E5002）；调 `bailianService.askSync(prompt, 'content-gen')` 生成草稿，**强制引用标注**（citations 记录每条事实来源 record_id）；写入 data_content（version=1, status=draft）+ data_content_citations）
   - `POST /api/content/:id/regenerate`（body：{prompt_hint?}；version+1，generation_history 追加；重新生成覆盖 content/citations）
   - `POST /api/content/:id/edit`（status draft→editing，进入人工修改中间态）
   - `POST /api/content/:id/finalize`（status → confirmed 定稿；**状态流转为 draft→editing→confirmed**：前端点 [修改] 转 editing，[定稿] 仅允许从 draft/editing 到 confirmed）
   - `GET /api/content/:id`（内容 + citations 详情）
   - `GET /api/content?channel=&style=`（列表）
3. 渠道×风格兼容矩阵（05 文档 2.3）：

| 渠道 | 新闻简讯 | 日评 | 深度报道 |
|---|---|---|---|
| 51号APP | ✅ | ✅ | △ |
| 公众号 | ✅ | ✅ | ✅ |
| 抖音/快手 | ✅ | △ | ❌ |
| 行业群 | ✅ | ❌ | ❌ |

（△ 允许但降级为摘要/脚本化；❌ 直接拒绝）
4. 素材包组装规则："已有即引用、缺失不补造"（链上无 forecast 就不写预测内容）

**验收**：`npm run type:check` 通过；可用一条已确认事件生成公众号深度报道 + 51APP 卡片 + 行业群快讯 3 个版本。

---

## Agent 5 — 前端工作台三页（erp/data-collect/workbench/）

**角色**：采集工作台（首页 / 处理页 / 我的记录页）。

**文件所有权（独占）**：`erp/data-collect/workbench/` 整个目录（index.vue + api.ts + types.ts）。
**禁止触碰**：router/index.ts（Agent 8 注册路由）、其他 erp 页面。

**任务**（页面结构参考 `erp/xiuma-data-import/index.vue` 与 `erp/cache-admin/index.vue` 的 fetch 模式）：
0. **ERP 前端栈声明（首条必读）**：ERP 现有页面为 **Element Plus 组件 + scoped CSS**（非 Tailwind）；本项目允许为本功能自由设计 UI（用户明确授权可不沿用系统主题，要求美观），但组件优先复用 Element Plus，减少依赖风险
1. **首页**：今日待处理 N 篇（按状态分组卡片）+ 类型分布 + 快捷入口（提交文章弹窗：粘贴/上传/URL）
2. **处理页**（核心，黄金路径主战场）：
   - 左侧：文章原文（可滚动）
   - 右侧：结构化表单（AI 预填；**三态呈现**：🟢 正常直接显示值 / 🟡 黄色高亮 + [AI依据] 链接（点开原文片段）/ 🔴 红色 + [必须确认]）
   - 判断类字段（影响程度等）展示：AI 判断值 + [采纳建议]/[保持 AI 结果] 按钮
   - 顶部状态条：🟢 x 项 / 🟡 x 项 / 🔴 x 项
   - 底部操作条：一键全过（被硬拦截时禁用并提示原因）/ 提交 / 存草稿 / 跳过 / 放弃
   - mixed 文章：子记录标签页切换
3. **我的记录页**：已完成列表（标题/类型/状态/校核人/时间）+ 展开看子记录
4. api.ts 封装：GET 用 `cache: 'no-store'` + Authorization；POST 带 Content-Type + Authorization；**飞书环境所有请求（含 GET）必须带 `x-suda-csrf-token`（取自 `document.cookie` 的 `suda-csrf-token`）**——⚠️ 参考页 `erp/xiuma-data-import/index.vue` 的 fetch 漏了该 header，不要照抄这一点；正确范例见 `client/src/api/trade.ts` 的 `tradeGet`

**验收**：页面可操作；用 mock 数据可走完"打开→看标红→改 1 个字段→一键全过→确认"流程。

---

## Agent 6 — 前端图谱页（erp/data-collect/kg-graph/）

**角色**：图谱可视化页（AntV G6，5 个核心功能）。

**文件所有权（独占）**：`erp/data-collect/kg-graph/` 整个目录。
**禁止触碰**：router/index.ts、其他文件。

**任务**：
0. **ERP 前端栈声明（首条必读）**：ERP 现有页面为 **Element Plus 组件 + scoped CSS**（非 Tailwind）；本项目允许为本功能自由设计 UI（用户明确授权可不沿用系统主题，要求美观），但组件优先复用 Element Plus，减少依赖风险
1. 图谱页（仅 5 功能，**不做**高亮路径/PNG 导出/动画/聚合/筛选）：
   - ① 实体搜索框（调 /api/kg/search）→ 定位节点并展开 1-2 跳子图
   - ② 子图展开：点击节点 +1 跳（调 /api/kg/subgraph，增量合并 nodes/edges）
   - ③ 节点详情侧栏：名称/类型/别名/描述/来源
   - ④ 视觉区分：node_category=entity 方形、record 六边形；边按 relation_type 配色（CAUSED_BY 红、IMPACTS 橙、REFERS_TO 灰、PREDICTS 蓝、VERIFIES 绿）
   - ⑤ 顶部统计（调 /api/kg/stats：节点/关系总数 + 类型分布）
2. **@antv/g6 v5 已安装（人工执行 `npm pkg set dependencies.@antv/g6="^5.0.0" && npm install` 成功），直接 `import` 使用即可，无需再装依赖、不得改 client/index.html 走 CDN**。⚠️ Agent 6 必须使用 **G6 5.x API**（Graph 配置、behaviors、插件 API 与 4.x 差异大，勿照抄旧示例/旧文档代码）
3. 参考页面：`erp/` 现有页面的布局风格（顶部标题栏 + 主内容区）；ERP 蓝色主题 #2f71ff

**验收**：搜索"太钢"→ 子图渲染 → 点节点展开 → 详情侧栏出现 → 统计数字正确。

---

## Agent 7 — 前端内容编辑工作台（erp/data-collect/content-studio/）

**角色**：素材选择 → 渠道/风格选择 → AI 生成 → 人工修改/重生成 → 定稿。

**文件所有权（独占）**：`erp/data-collect/content-studio/` 整个目录。
**禁止触碰**：router/index.ts、其他文件。

**任务**：
0. **ERP 前端栈声明（首条必读）**：ERP 现有页面为 **Element Plus 组件 + scoped CSS**（非 Tailwind）；本项目允许为本功能自由设计 UI（用户明确授权可不沿用系统主题，要求美观），但组件优先复用 Element Plus，减少依赖风险
1. 素材选择区：已确认事件/数据列表（多选 checkbox，只显示 confirmed/active——后端会再次校验）
2. 生成配置区：渠道多选（公众号/51APP/抖音/行业群）+ 风格单选（新闻简讯/日评/深度报道）；**无效组合置灰**（兼容矩阵 05 文档 2.3）
3. 生成结果区：点击"生成内容"→ 调用 /api/content/generate → 多个渠道版本并排展示（标题+正文+引用标注）
4. 操作区：[重新生成]（换风格/更专业/更口语，version 递增）/ [修改]（可编辑文本，状态转 editing）/ [保存草稿] / [定稿]
5. 明早演示 3 个成品：公众号深度报道 / 51APP 卡片 / 行业群快讯（页面默认展示这三个渠道版本即可）

**验收**：选 1 条已确认事件 → 勾 3 渠道 → 生成 3 版本 → 改一段 → 定稿成功。

---

## Agent 8 — 集成收尾（最后启动，等 1-7 完成）

**角色**：模块注册、路由注册、侧边栏菜单、CSRF/鉴权核对、type:check、联调、明早演示数据准备、开发日志汇总。

**文件所有权（独占）**：`server/app.module.ts`、`client/src/router/index.ts`（仅追加 ERP 子路由）、`erp/server/erp-sidebar.service.ts`（仅 MENU_ITEMS 追加）、`docs/AI采集标注/开发日志.md`（汇总新建）。
**禁止触碰**：Agent 1-7 名下文件（只读检查，不改业务逻辑；发现 bug 按分级处理，见验收）。

**任务**：
1. app.module.ts 注册：CollectModule、KgModule、ContentModule（imports.push 追加；三个模块均位于 `erp/data-collect/server/` 下，按实际相对路径 import）
2. router/index.ts 追加 ERP 子路由（ErpLayout children）：
   - `/erp/data-collection` → erp/data-collect/workbench/index.vue（title: 采集工作台）
   - `/erp/kg-graph` → erp/data-collect/kg-graph/index.vue（title: 知识图谱）
   - `/erp/content-studio` → erp/data-collect/content-studio/index.vue（title: 内容生成工作台）
3. erp-sidebar.service.ts MENU_ITEMS 追加三项（采集工作台/知识图谱/内容生成工作台）
4. 全局核对：所有新后端接口是否都带 ErpAuthGuard（且模块 providers 均含 ErpAuthGuard+ErpAuthService）；**前端所有请求（含 GET）是否带 `x-suda-csrf-token` header（飞书环境，取自 document.cookie 的 suda-csrf-token）**
5. `npm run type:check` 全项目通过
6. 准备演示数据：用 Agent 1 的字典 + 一篇真实测试文章（同事提供的 5-10 篇中选 1-2 篇，可放在 `erp/data-collect/` 同级临时目录或直接通过 API 提交）跑通黄金路径
7. 输出《交付说明》：遗留问题、未完成项、mock 兜底清单（各 Agent 报告汇总）、开发日志汇总（写入 `docs/AI采集标注/开发日志.md`）。**上线交接警告（必须写入）**：① online 库 033 迁移冻结期严禁 `miaoda deploy`；② 新表 SQL 上飞书须按单语句 ≤5000 行/1MB、30 秒超时限制分批执行，且**须先在 online 库执行 033 之后再发布**
8. **bug 分级判据（验收时执行）**：P0 级（应用崩溃/黄金路径链路断裂）必须修复后交付；P1 级（体验/边界问题）记录交付说明即可

**验收**：黄金路径端到端跑通：提交文章 → AI 识别 → 结构化 → 人工改 ≤3 处 → 入库 → 图谱可见 → 生成 3 个成品。

---

# 第 2 部分：全局验收与交付

## 2.1 明早演示清单（Agent 8 最后核对）

| 项 | 是否就绪 |
|---|---|
| 一篇文章全流程（采→存）| |
| 图谱页搜索"太钢"展开链路（图）| |
| 内容编辑工作台生成 3 成品（出）| |
| 关键事实错误率抽测——**金标准测试集依赖同事提供，智能体不可自产；本项移交人工，交付说明中记录** | |

## 2.2 交付物

- 各 Agent 名下代码（type:check 通过），按 §0.4 红线 6 的提交规范分里程碑 commit
- Agent 8 汇总《交付说明》：遗留问题、mock 兜底清单、演示操作步骤（从启动到演示的逐步操作）、上线交接警告（§Agent 8 任务 7）
- Agent 8 汇总 `docs/AI采集标注/开发日志.md`（各 Agent 踩坑/困难/决策点）

## 2.3 依赖说明（无需 Agent 操作）

- @antv/g6 v5：**已由人工安装**（`npm pkg set dependencies.@antv/g6="^5.0.0" && npm install` 执行成功），Agent 直接 import 使用，勿再改 package.json、勿走 CDN

---

# 第 3 部分：Agent 协作速查

| Agent | 依赖谁 | 提供什么 | 联调顺序 |
|---|---|---|---|
| 1 数据库 | 无 | Repository 签名、表结构；**交付门槛 = 035~040 SQL 已在本地库执行成功 + COUNT 校验通过** | 最先 |
| 2 采集后端 | 1 | 采集 API | 依赖 1 |
| 3 图谱后端 | 1 | 图谱 API | 依赖 1、2（演示数据） |
| 4 内容后端 | 1 | 内容 API | 依赖 1、2（素材） |
| 5 前端工作台 | 2 的 API 契约 | 工作台页面 | 可 mock 先行 |
| 6 前端图谱 | 3 的 API 契约 | 图谱页 | 可 mock 先行 |
| 7 前端内容 | 4 的 API 契约 | 内容工作台 | 可 mock 先行 |
| 8 集成 | 全部 | 注册/联调/演示 | 最后 |

**并行要点**：后端 2/3/4 按上方 Repository 方法签名开发（Agent 1 实现后即可联调）；前端 5/6/7 用 mock 数据先行开发页面，API 联调在 Agent 8 阶段统一进行。所有新代码集中在 `erp/data-collect/`（见 §0.6）；每完成一个里程碑按 §0.4 红线 6 提交 git；全程严禁 `miaoda deploy` 与触碰 online 库。

---

*文档版本：V1.1 ｜ 2026-08-20 ｜ 依据 00-08 方案文档 ｜ 明早 MVP 黄金路径落地*
