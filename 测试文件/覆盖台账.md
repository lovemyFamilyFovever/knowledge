# 知库彻查 · 覆盖台账

> 用途：把"查过了"从主观变成可计数。**每轮彻查开工前先读本文件，只碰"未测"项；收工必须更新本文件并给出覆盖数字。**
> 状态取值：`未测` / `单测` / `E2E` / `门禁`(pre-commit 或 CI 拦得住) / `人工` / `不适用+理由`
> 严重度：`数据丢失` > `功能错误` > `交互误导` > `美观`

**P0 填写口径（2026-09-23，重要）**
- P0 只做**枚举**，不执行任何行为验证。因此**除 §1 中"已有 smoke 真实发 HTTP 请求"的端点记为 `单测` 外，本轮所有格子一律 `未测`**。
- §1 `有测试?` 的判定标准是机器可复现的硬标准：该路径**以字面量形式出现在 `tests/*.py` 的真实 HTTP 调用里**（`app.test_client()` + `.get/.post("...")`），且调用点附近有 `check(...)` 断言。字符串出现在注释/文档里不算。
- §2 的四态列（正常/空/加载/失败）本轮**一律记 `未测`**，包括结构上不适用的控件——不设主观豁免，避免"我觉得它没有空态"式推断混进结论。四态由 P4/P8 填写。
- §4 的"可执行化?"回答的是"**现在是否已有**可跑断言"，**不是**"理论上能否做到"。因此 P0 阶段 8 条全为 `否`；P2 的产出目标是把它们变成 `是`。
- 本轮**未执行**任何 UI / 服务器 / 网络 / 装包操作；结论全部来自只读命令输出与代码通读。

---

## 0. 本轮汇总（每轮覆盖式更新，保留历史）

| 轮次 | 日期 | 模型/会话 | 跑的阶段 | 总格子 | 已覆盖 | 未覆盖 | 新发现(按严重度) | 落了多少回归断言 |
|---|---|---|---|---|---|---|---|---|
| 1 | 2026-09-23 | Qoder CN 会话（只读彻查） | P0 | 199 | 33 | 166 | 无（P0 为枚举阶段，未产生经实证的缺陷；P0 自身不计入回归断言） | 0（P0 不写断言） |
| 2 | 2026-09-23 | Qoder CN 会话 | P1 | 199 | 33 | 166 | 1（工具链缺口，功能错误级：**CI 覆盖面小于 pre-commit**——`ci.yml` 不跑 ruff，且 5 套 smoke 只跑 3 套。见 §6 第 1 行） | 0（P1 产物是门禁本身而非断言：§7 中 eslint/stylelint 两行由「否」→「是」，另关闭 1 条已确认缺口） |
| 3 | 2026-09-23 | Qoder CN 会话 | P2 | 199 | 41 | 158 | 1（测试缺口，数据丢失级（潜在）：`/api/inbox/purge` 是全仓唯一物理删除正式路径的接口，其越界防护**无任何回归断言**——代码注释记载曾因路径穿越误删 1 篇正式文档。见 §6 第 2 行） | **59 条**（`tests/test_invariants.py` 8 组；另 1 个静态门禁脚本 `scripts/check_rag_version.py`） |

**单调性检查**：本轮"未覆盖"数必须 ≤ 上一轮，否则说明清单被改大或有格子被划掉——需在 §6 说明原因。

> 轮次 1 数字构成：总格子 = §1 端点 58 + §2 UI 控件 110 + §3 文件类型 10 + §4 不变量 8 + §5 解析入口 13 = **199**；
> 已覆盖 33 全部来自 §1（smoke 有真实 HTTP 调用 + 断言）；未覆盖 166 = 199 − 33（**全部记 `未测`**）。
> 计数由脚本对本文档 §1~§5 表格逐行统计得出（按各表的"状态"列取值），非手数。
>
> 轮次 2（P1）数字构成不变：P1 的收益落在 **§7 门禁层**（不属 199 个业务格子，故不计入覆盖数）——
> §7 共 8 行，接进 pre-commit 的由 4 行增至 **6 行**（新增 eslint、stylelint），并关闭"eslint 漏 `static/kb-novel.js`"这条已确认缺口。
> 业务格子（§1~§5）本轮**一格未动**：P1 不执行行为验证，无状态可升级，故仍为 33 / 166。
>
> 轮次 3（P2）数字构成：**§4 的 8 格由 `未测` 全部翻为 `门禁`**（可执行断言 + 进 pre-commit + 进 CI），
> 故已覆盖 33 → **41**（+8），未覆盖 166 → **158**（−8，单调下降 ✓）。
> 其余四张表（§1 端点 58 / §2 UI 110 / §3 文件类型 10 / §5 解析入口 13）本轮**未动**，理由：P2 只做不变量门禁，不执行端点/UI/解析器的行为验证。
> 本轮同时新增 1 条守卫断言组（I4 的 purge 越界）与 1 个静态门禁脚本（I7），均落在 §7 与 §4，不计入 199 格子。

---

## 1. 端点清单（P0 用 `create_app().url_map` 机器生成后填入）

**枚举命令（P0 实跑，`KB_ROOT` 指向一次性 temp 目录，见 §9 注 1）**

```
python -c "import sys;sys.path.insert(0,'.');from app.app import create_app;app=create_app();[print(sorted(r.methods-{'HEAD','OPTIONS'}),r.rule,'->',r.endpoint) for r in sorted(app.url_map.iter_rules(),key=lambda x:x.rule)]"
```

**原始结果（`/static/<path:filename>` 为 Flask 内建，不计入业务端点）**

```
TOTAL_RULES=59
TOTAL_NONSTATIC=58
```

**与装饰器数量对照（原始输出）**

```
$ grep -rhoE '@[a-z_]+_bp\.(get|post|delete|put|route)\("[^"]+"' app/*.py | wc -l
58
$ for f in app/routes_*.py; do grep -coE '...' "$f"; done
app/routes_ai.py 2    app/routes_doc.py 2    app/routes_edit.py 7
app/routes_files.py 8 app/routes_learn.py 11 app/routes_pages.py 11
app/routes_rag.py 2   app/routes_search.py 6 app/routes_stats.py 9   （合计 58）
```

**结论：58 == 58，集合逐条对齐，无差异。** 补充核验：
- 装饰器 URL 去重后 57 条 vs `url_map` 去重后 57 条，`ONLY_IN_DECORATORS: (none)`、`ONLY_IN_URL_MAP: (none)`；
  差的 1 条是 `/api/docmark` —— 它在 `routes_stats.py:278[GET]` 与 `routes_stats.py:295[POST]` 各挂一次装饰器，
  是一条 URL 两个方法两条 rule，**不是**重复注册或死路由。
- 全仓 `grep -rnE 'add_url_rule|@[a-z_]+\.route\(' app/*.py` 只命中带 `_bp.` 前缀的正常装饰器，**无动态注册、无被注释掉的死路由**。

| # | Method | Rule | endpoint | 有测试? | 状态 | 备注 |
|---|---|---|---|---|---|---|
| 1 | GET | / | pages.index | test_reader.py | 单测 | 占位页渲染 landing.html |
| 2 | POST | /api/ask | ai.api_ask | 无 | 未测 | AI 问吧；tests/scripts 零引用 |
| 3 | GET | /api/ask/status | ai.api_ask_status | 无 | 未测 | 同上 |
| 4 | POST | /api/delete | files.api_delete | test_reader.py, api_patrol.py | 单测 | 软删 → `_trash/<ts>/` |
| 5 | GET | /api/dir/tree | stats.api_dir_tree | test_reader.py | 单测 | |
| 6 | GET | /api/doc | doc.api_doc | test_reader.py | 单测 | |
| 7 | GET | /api/docmark | stats.api_docmark_get | test_reader.py | 单测 | |
| 8 | POST | /api/docmark | stats.api_docmark_set | test_reader.py | 单测 | 同 URL 第二方法 |
| 9 | POST | /api/favorite | edit.api_favorite | test_reader.py, api_patrol.py | 单测 | |
| 10 | GET | /api/globalstats | stats.api_globalstats | test_govern.py, test_reader.py | 单测 | |
| 11 | GET | /api/glossary | search.api_glossary | test_learn.py | 单测 | |
| 12 | GET | /api/governance/scan | stats.api_governance_scan | 无 | 未测 | 治理驾驶舱唯一数据源 |
| 13 | POST | /api/import | edit.api_import | 无 | 未测 | multipart 批量导入 |
| 14 | POST | /api/inbox/ignore | files.api_inbox_ignore | 无 | 未测 | 写忽略清单 |
| 15 | POST | /api/inbox/purge | files.api_inbox_purge | 无 | 未测 | 收件箱清理（破坏性待判） |
| 16 | GET | /api/learn/cards | learn.api_cards | test_learn.py | 单测 | |
| 17 | GET | /api/learn/due | learn.api_due | test_learn.py | 单测 | |
| 18 | GET | /api/learn/mastery | learn.api_mastery | test_learn.py | 单测 | |
| 19 | GET | /api/learn/mock | learn.api_mock | 无 | 未测 | 测验抽题 |
| 20 | GET | /api/learn/recent_read | learn.api_recent_read | 无 | 未测 | |
| 21 | POST | /api/learn/review | learn.api_review | test_learn.py | 单测 | |
| 22 | GET | /api/learn/roam | learn.api_roam | test_learn.py | 单测 | |
| 23 | POST | /api/learn/sync | learn.api_sync | test_learn.py | 单测 | |
| 24 | GET | /api/learn/today | learn.api_today | test_learn.py | 单测 | |
| 25 | GET | /api/links | edit.api_links | test_reader.py, api_patrol.py | 单测 | |
| 26 | POST | /api/mkdir | edit.api_mkdir | 无 | 未测 | 建目录 |
| 27 | POST | /api/move | files.api_move | test_reader.py, api_patrol.py | 单测 | |
| 28 | POST | /api/move/batch | files.api_move_batch | 仅 api_patrol.py | 未测 | 巡检脚本需常驻 5001，非门禁 |
| 29 | POST | /api/note | edit.api_note | test_reader.py, api_patrol.py | 单测 | |
| 30 | GET | /api/palette/index | search.api_palette_index | test_learn.py | 单测 | |
| 31 | GET | /api/rag | rag.api_rag | 仅 api_patrol.py | 未测 | 本机缺 tokenizers/onnxruntime，实际跑不了 |
| 32 | GET | /api/rag/status | rag.api_rag_status | 无 | 未测 | |
| 33 | POST | /api/rename-domain | files.api_rename_domain | test_reader.py | 单测 | |
| 34 | POST | /api/rename-sub | files.api_rename_sub | 无 | 未测 | 子域改名迁移 |
| 35 | POST | /api/rmdir | files.api_rmdir | 无 | 未测 | **整树软删**，破坏性最高 |
| 36 | POST | /api/save | edit.api_save | test_reader.py, api_patrol.py | 单测 | 写回唯一事实源 |
| 37 | GET | /api/search | search.api_search | test_learn.py | 单测 | |
| 38 | GET | /api/stats | stats.api_stats | 仅 api_patrol.py | 未测 | |
| 39 | GET | /api/substats | stats.api_substats | 仅 api_patrol.py | 未测 | |
| 40 | POST | /api/tag/merge | stats.api_tag_merge | 仅 api_patrol.py | 未测 | 全库标签改写 |
| 41 | POST | /api/tags | edit.api_tags | 无 | 未测 | |
| 42 | POST | /api/track | stats.api_track | 仅 api_patrol.py | 未测 | 阅读事件落库 |
| 43 | GET | /api/tree | doc.api_tree | test_reader.py | 单测 | |
| 44 | POST | /api/wikilink/check | search.api_wikilink_check | test_learn.py | 单测 | |
| 45 | GET | /api/wikilink/suggest | search.api_wikilink_suggest | test_learn.py | 单测 | |
| 46 | GET | /browse/&lt;domain&gt;/&lt;sub&gt; | pages.browse | test_reader.py | 单测 | |
| 47 | GET | /doc/&lt;domain&gt;/&lt;sub&gt;/&lt;path:name&gt; | pages.doc | test_reader.py | 单测 | |
| 48 | GET | /favorites | pages.favorites | test_reader.py | 单测 | |
| 49 | GET | /glossary | search.page_glossary | 无 | 未测 | 术语门户页 |
| 50 | GET | /governance | pages.governance | 无 | 未测 | |
| 51 | GET | /home | pages.home | test_reader.py | 单测 | |
| 52 | GET | /inbox | pages.inbox_page | 无 | 未测 | |
| 53 | GET | /quiz | learn.page_quiz | 无 | 未测 | |
| 54 | GET | /raw/&lt;path:rel&gt; | pages.raw | test_reader.py | 单测 | 含穿越拒绝用例 |
| 55 | GET | /review | learn.page_review | 无 | 未测 | |
| 56 | GET | /search | pages.search_route | test_reader.py, api_patrol.py | 单测 | |
| 57 | GET | /stats | pages.stats_page | 无 | 未测 | |
| 58 | GET | /tags | pages.tags | test_reader.py | 单测 | |

**汇总：单测 33 / 仅巡检脚本 6 / 完全无引用 19 / 合计 58。**

> 填完后与 `grep -rhoE '@[a-z_]+_bp\.(get|post|delete|put|route)\("[^"]+"' app/*.py | wc -l` 对照；数量不符本身就是发现（动态注册 / 死路由 / 注释掉的）。
> **本轮对照结果：相符（58 = 58），无发现。**

---

## 2. UI 控件清单（P0 从 templates + static 提取）

**提取范围（原始命令）**

```
grep -rhoE 'data-act="[^"]+"' app/templates/ static/*.js static/pages/*.js      # 仅 7 处（收件箱/复习页）
grep -rhoE 'onclick="[^"]+"'  app/templates/*.html                              # 16 处
grep -rhoE 'id="[^"]+"'       app/templates/*.html                              # 模板层 id
grep -nE '\.key *==|id *= *"|getElementById\(' static/{app,kb-core,kb-novel}.js static/pages/*.js
```

**结构事实（先记，避免下一轮重复摸）**

- 主体 UI 不在模板里：`workbench.html` 仅 129 行、`base.html` 283 行；顶栏/左树/读数头/右栏/编辑器的骨架在模板，**交互全在 JS**。
- 控件装配三处：`app.js`（树/读数头/crumb/正文/右键菜单/编辑器/就地搜索浮层）、`kb-core.js`（命令面板 Ctrl+K、设置抽屉、`[[` 补全、帮助）、`kb-novel.js`（小说工具栏、书内搜索、章评、TTS、自动滚动、下载）。
- **右栏实为 4 个页签**（目录/标签/备注/双链），不是手册 P0 第 2 条写的"五页签"——见 §9 注 2。
- **设置抽屉实为 4 个页签**（外观/排版/小说/快捷键），与手册一致。小说页签内容由 `KBNOVEL.panelHTML()` 注入 `#nv-panel-host`（`kb-core.js:384`）。
- 快捷键有**唯一数据源** `KEY_REGISTRY`（`kb-core.js:856`，14 条，含 scope：global/browse/editor/review），`?` 帮助面板与设置抽屉快捷键页都由它派生。
- `data-act` 只在收件箱/复习页用（move/ignore/del/purge/roam/review/doc，共 7 处），不是全局机制。

| 区域 | 控件/入口 | 正常态 | 空态 | 加载态 | 失败态 | 破坏性? | 状态 |
|---|---|---|---|---|---|---|---|
| 顶栏 | brand → /home | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 一级导航 · 阅读(/) | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 一级导航 · 复习(/review) | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 一级导航 · 术语(/glossary) | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 一级导航 · 统计(/stats) | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 问吧 showAsk()（→ #ask-in/#ask-go/#ask-body/#ask-empty/#ask-pending） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 收藏(/favorites) | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 标签(/tags) | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 治理(/governance) | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 搜索框 #q（Enter → /search?q=） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | #global-stats-btn → showGlobalStats()（#gs-body） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 收件箱入口(/inbox + 计数 badge) | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | #theme-btn 明暗切换（右键直达设置） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | #kb-settings-btn 设置抽屉 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | 状态栏 #sb-path + 篇数计数 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 顶栏 | #toast 全局提示 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 左分类树 | fold/unfold togglePanel('left') | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 左分类树 | #tree-total 篇数 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 左分类树 | 域头 .dom-head 展开/收起 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 左分类树 | 子域 .sub 展开/收起 + 跳转 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 左分类树 | 域内文档 .doc 打开 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 左分类树 | 域右键菜单（新建文档/新增二级目录/导入/重命名域/复制域名/删除域） | 未测 | 未测 | 未测 | 未测 | 是（删除域） | 未测 |
| 左分类树 | 子域右键菜单（在此新建文档/新建子目录/导入/重命名目录/删除目录；_root 只给前 3 项） | 未测 | 未测 | 未测 | 未测 | 是（删除目录） | 未测 |
| 左分类树 | 文档右键菜单（统计信息/复制原文·站内链接·相对路径/移动到…/重命名…/删除…） | 未测 | 未测 | 未测 | 未测 | 是（删除） | 未测 |
| 读数头 crumb | #crumb-tags 标签 chips | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 读数头 crumb | #kb-md-src-btn toggleMdSource() | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 读数头 crumb | 新标签页（/raw/&lt;html_twin&gt;） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 读数头 crumb | 编辑 openEditor() | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 读数头 crumb | 删除 deleteDoc() | 未测 | 未测 | 未测 | 未测 | 是 | 未测 |
| 读数头 crumb | 标签 jumpToTagEdit() | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 读数头 crumb | #fav-btn toggleFav() | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | #article 渲染容器（enhanceArticleDOM 后处理） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | #kb-toc-spark 目录火花 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | #kb-finish-bar 读完标记 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | #kb-empty-newdoc 空态入口 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | #html-render 美化版内嵌渲染 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | 编辑器 · 取消 tryCloseEditor() | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | 编辑器 · #ed-del 删除 | 未测 | 未测 | 未测 | 未测 | 是 | 未测 |
| 正文区 | 编辑器 · 保存写回 saveDoc() | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | 编辑器 · #ed-text（CodeMirror 桥接） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | 编辑器 · #ed-hint frontmatter 提示 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | cm-editor.js 格式工具栏 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | [[ 双链补全（#kb-wl-bar / #kb-wl-sug） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | 标签补全浮层（tag-suggest.js） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 正文区 | mermaid 点击放大 #kb-zoom-ov（服务端注入片段） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 搜索浮层 | #kb-search-ov 容器 / #kb-so-q 输入（Esc 关） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 搜索浮层 | #kb-so-clear 清空 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 搜索浮层 | 引擎 chips ×4（hybrid/fts/semantic/tags） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 搜索浮层 | 范围 chips ×11（全部域 + 7 域 + 仅收藏 + 仅未掌握） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 搜索浮层 | #kb-so-body 结果区（Enter 打开首条） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 右栏·目录 | #pane-toc 章节列表 + 跳转（小说模式由 KBNOVEL 灌入） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 右栏·标签 | .tagchip-x removeTag() | 未测 | 未测 | 未测 | 未测 | 是（删标签） | 未测 |
| 右栏·标签 | #tag-add-btn / #tag-inputrow / #tag-in | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 右栏·标签 | 元信息表（来源/原始位置/收录日期/状态/大小） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 右栏·标签 | 美化版只读态（doc.is_html 时不显示编辑/删除） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 右栏·备注 | #ni + 记录 addNote() | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 右栏·双链 | #pane-links loadLinks()（打开页签时加载） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 右栏 tabs | rtab ×4 + 计数 #rt-n-tags/#rt-n-notes/#rt-n-links | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·外观 | 皮肤格 KB_SKINS ×6（玄青/纸墨/午夜蓝/手卷/叠印/石墨） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·外观 | #kb-pref-motion 强制开启动画 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·外观 | #kb-pref-reset 恢复默认 | 未测 | 未测 | 未测 | 未测 | 是（重置偏好） | 未测 |
| 设置·排版 | #kb-pref-scale 正文字号 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·排版 | #kb-pref-line 行高 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·排版 | #kb-pref-h1s / h2s / h3s 标题字号 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·排版 | #kb-pref-code 代码字号 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·排版 | #kb-pref-halign 标题对齐 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·排版 | #kb-pref-font 正文字体 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·排版 | #kb-pref-fontsys 读取系统字体（需授权） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·小说 | #kb-npref-size/line/track/gap/widthRem 排版五项 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·小说 | #kb-npref-indent 首行缩进 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·小说 | #kb-npref-font + #kb-npref-fontsys | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·小说 | 护眼主题按钮组 THEMES + #kb-npref-bgcustom 自定义底色 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·小说 | #kb-npref-flow txt 阅读模式（滚动/分页） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·小说 | #kb-npref-rate 朗读/滚动速度 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 设置·小说 | #kb-npref-reset 恢复默认 | 未测 | 未测 | 未测 | 未测 | 是（重置偏好） | 未测 |
| 设置·快捷键 | 快捷键清单（KEY_REGISTRY 14 条，与 `?` 帮助同源） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 命令面板/帮助 | #kb-palette（Ctrl/Cmd+K：命令/术语/文档/子域） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 命令面板/帮助 | #kb-help（`?` 唤起完整键位表） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 小说阅读器工具栏 | .nv-prev-chap / .nv-next-chap（上章/下章，分页模式为上页/下页） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 小说阅读器工具栏 | #nv-pos 阅读位置 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 小说阅读器工具栏 | .nv-pref-btn → KB.settings.open('novel') | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 小说阅读器工具栏 | .nv-find-btn 书内搜索 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 小说阅读器工具栏 | .nv-note-btn 章评 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 小说阅读器工具栏 | .nv-tts 朗读本章（浏览器 TTS） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 小说阅读器工具栏 | .nv-auto 自动滚动（仅滚动模式出现） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 小说阅读器工具栏 | .nv-dl 下载原文件 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 收件箱 | #ib-all / .ib-check 选择 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 收件箱 | #ib-go 批量归档 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 收件箱 | mini-act data-act=move / ignore | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 收件箱 | mini-act data-act=del / purge | 未测 | 未测 | 未测 | 未测 | 是 | 未测 |
| 收件箱 | #inbox-kanban + #drawer-h / #drawer-docs 看板抽屉 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 复习 · 翻面 #kb-reveal + 评分 #kb-grades(1-4) + #kb-skip | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 复习 · 进度环 #kb-ring/#kb-ring-fg/#kb-ring-n/#kb-ring-l | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 复习 · 今日卡 #kb-today*（date/term/def/doc/tip/stats/show） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 复习 · 侧栏 #kb-side-stats / #kb-mastery-bars | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 复习 · 漫游 #kb-roam / #kb-roam-x | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 测验 · #kb-mock-start / #kb-mock-again / #kb-mock-exit / #kb-exact | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 术语 · #kb-gl-q / #kb-gl-clear | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 术语 · #kb-gl-buckets / #kb-gl-subs / #kb-gl-sub / #kb-gl-body | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 统计 · 月份 prev/next | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 统计 · #stDailyChart / #stChartNote / #st-daily-data | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 统计 · #stMasteryBody / #stMasterySum | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 治理 · #gov-scan-btn + 3 个 bucket 页签（dead_links/orphans/tag_pairs） | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 治理 · #dead-all / #dead-toplain / #dead-remove | 未测 | 未测 | 未测 | 未测 | 是（改写/移除断链） | 未测 |
| 复习/术语/统计/治理 | 治理 · #orphan-expand | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 标签页 · .t-check / #tag-selbar / #tag-sel-count / #tag-merge-sel / #tag-clear | 未测 | 未测 | 未测 | 未测 | 是（合并标签） | 未测 |
| 复习/术语/统计/治理 | 标签页 · .t-merge / #merge-suggest / #sugg-sec / #tag-drawer | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 标签页 · #tag-cloud | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 搜索页 · #kb-hits / #kb-facets / #kb-facet-clear | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |
| 复习/术语/统计/治理 | 首页 #btn-continue / 收藏页 / 错误页 | 未测 | 未测 | 未测 | 未测 | 否 | 未测 |

**§2 汇总：110 行，全部 `未测`。**（口径：一行 = 一个控件或一组同源微控件；`小说阅读器工具栏` 的 8 行只在 txt/epub 阅读态存在，`设置·小说` 的 7 行只在设置抽屉"小说"页签存在。）

---

## 3. 文件类型处理面

**权威常量（原始输出，`app/store.py:29-36`）**

```
29:FM_RE = re.compile(r"\A\ufeff?---[ \t]*\r?\n(.*?)\r?\n---[ \t]*(?:\r?\n)(?:\r?\n)?", re.S)
30:SKIP_DIRS = {"_inbox", "_assets", "_unfiled"}
31:WRITABLE_EXTS = {".md"}
32:SERVABLE_EXTS = {".md", ".html"}
33:# 需求 #12：书库格式 —— /raw 直服（阅读器按类型分流渲染），不进分类树/FTS/RAG
34:LIBRARY_EXTS = {".txt", ".epub", ".pdf", ".xlsx", ".mobi"}
35:# 需求#6：正文相对图片直服用的媒体扩展名（/raw/<rel> 允许，不进分类树/索引）
36:MEDIA_EXTS = {".png", ".jpg", ".jpeg", ".gif", ".webp", ".svg", ".ico", ".bmp", ".avif"}
```

**关键机制（代码位置，均只读通读所得）**

- 树的可见性：`store._visible_sub_files()`（`store.py:296`）收 `SERVABLE_EXTS | LIBRARY_EXTS`；注释注明"书库格式同样入树（阅读器分流渲染）"。
- 索引的可见性：`store.md_files()`（`store.py:286`）**只收 `.md`**；`fts.build_index()`（`fts.py:38`）走 `md_files`；`rag.md_corpus_files()`（`rag.py:447`）同样 `only fn.endswith(".md")` 且排除 `.notes.md`。
- 旁挂件命名：`store.notes_path()`（`store.py:542`）= `<doc>.md.notes.md`；同名美化版 = `<doc>.html`；清单由 `store.sidecars_of()`（`store.py:559`）给出，`rename_doc`（`store.py:569`）/`_side_plan`（`routes_files.py:75`）随行搬运。
- 可见性谓词：`store.is_visible_doc()`（`store.py:275`）统一排除 `_` 前缀段与 `.notes.md`（注释标注为 B2/B3 修复）。
- 删除路径：`routes_files.api_delete()`（`routes_files.py:51`）—— 主文件 + `.notes.md` + 同名 `.html` 三件一起 rename 进 `content/_trash/<YYYYmmdd-HHMMSS>/`，随后 `remove_doc_from_index`。
- 编辑路径：`routes_edit.api_save()`（`routes_edit.py:41`）后缀白名单 `WRITABLE_EXTS`（仅 .md），并额外拒绝任何 `_` 前缀路径段（`routes_edit.py:44`）。
- `/raw` 服务面：`routes_pages.raw()`（`routes_pages.py:188`）= `SERVABLE_EXTS | MEDIA_EXTS | LIBRARY_EXTS`；`.html` 走 `_rewrite_html_assets` 重写依赖后返回，其余 `send_file`。

| 扩展名 | 入口渲染 | 可编辑 | 删除路径 | 进 FTS? | 进 RAG? | 旁挂件正确排除 | 状态 |
|---|---|---|---|---|---|---|---|
| .md | 是 · `/doc/*` 走 `collect_doc`；`pages.raw` 直服 | 是（`WRITABLE_EXTS` 全库唯一可写） | 软删（api_delete，含 2 件旁挂） | 是（md_files） | 是（md_corpus_files） | 是（is_visible_doc） | 未测 |
| .html | 是 · 双通道：独立成篇走 `/doc`（`doc.is_html`），或作美化版经 `/raw` 内嵌 | 否（模板对 `doc.is_html` 隐藏编辑/删除/标签） | 随主文件软删；纯 .html 独立篇按 `/api/delete` | 否（build_index 只走 md_files） | 否 | 是（不产 .notes.md，作 twin 随主文件搬运） | 未测 |
| .txt | 是 · `/raw` 直服 → `KBNOVEL.renderTxt` 前端渲染 | 否 | 随目录/域 rmdir 或 `/api/delete`（后端 exts=WRITABLE_EXTS，见备注 A） | 否 | 否 | 是（sidecars_of 天然不含 .txt） | 未测 |
| .epub | 是 · `/raw` 直服 → `normalizeEpub` + `renderEpub` | 否 | 同上 | 否 | 否 | 是 | 未测 |
| .pdf | 是 · `/raw` 直服（浏览器内建查看器，无前端解析） | 否 | 同上 | 否 | 否 | 是 | 未测 |
| .xlsx | 是 · `/raw` 直服（下载/浏览器行为，无前端解析） | 否 | 同上 | 否 | 否 | 是 | 未测 |
| .mobi | 是 · `/raw` 直服（**无专用渲染器**，KB-NOVEL 未处理 .mobi） | 否 | 同上 | 否 | 否 | 是 | 未测 |
| .notes.md（旁挂） | 否（is_visible_doc 排除，不进树/不进 /doc） | 通过右栏备注 UI 追加（api_note） | 随主文件软删（api_delete 显式搬第二件） | 否（md_files 排除） | 否（md_corpus_files 排除） | —（本身就是旁挂件） | 未测 |
| 同名 .html（美化版） | 是 · crumb「新标签页」+ `#html-render` 内嵌 | 内容由外部工具生成后放入，站内不可编辑 | 随主文件软删（api_delete 显式搬第二件） | 否 | 否 | —（本身就是旁挂件） | 未测 |
| .png/.jpg/…（MEDIA_EXTS） | 是 · `/raw` 直服（正文相对图片） | 否 | 不在 doc/trash 流程内 | 否 | 否 | — | 未测 |

**§3 汇总：10 行**（7 主扩展名 + 2 旁挂件 + 1 媒体类），全部 `未测`。

**备注 A（待 P4 实证，勿当结论）**：`api_delete` 的后缀白名单是 `WRITABLE_EXTS`（仅 `.md`），而 `.txt/.epub/.pdf/.xlsx/.mobi` 是"能进树、能在阅读器打开、但**不能经 `/api/delete` 单篇软删**"。UI 侧 `deleteDoc()` 只在 `not is_lib` 分支渲染（`workbench.html:50-52`），所以前端不会走到——**两条路径是否自洽、书库文件在 UI 上如何删除，本轮未实测，标未验证**。

---

## 4. AGENTS.md 不变量 → 可执行化（P2 已完成 · 2026-09-23）

> 8 条**全部**可执行化，无一落"不可执行"。断言在 `tests/test_invariants.py`（独立脚本风格，`tempfile` 迷你语料，绝不读写真实 `content/`），
> 已接进 `.githooks/pre-commit` 与 `.github/workflows/ci.yml`。**实测 59 passed / 0 failed / 0 skipped**。

| # | 不变量摘要 | 可执行化? | 断言落在哪（函数） | 断言数 | 进 pre-commit/CI? | 状态 |
|---|---|---|---|---|---|---|
| 1 | content/ 唯一真相，编辑/收藏/备注写回文件系统 | **是** | `test_invariants.py::test_i1`：api_save/api_note/api_favorite 后断磁盘字节真的变（重新 parse 而非读进程内状态）；断 root 下除 `content/`、`indexes/` 外无任何落盘；断 `content/` 无 `.db`；静态扫 app/ 所有 `sqlite3.connect` 目标必须落在 `indexes/` | 8 | 是 / 是 | **门禁** |
| 2 | `_` 前缀目录不进树、不进索引 | **是** | `test_i2`：临时语料放 `_foo/ _inbox/ _assets/` + 嵌套 `ai/_tmp/`，断 `md_files` / `/api/dir/tree` / `find_doc` / `/doc` 路由 / FTS / RAG 语料集六处都不含它们，而可见文档六处都在 | 8 | 是 / 是 | **门禁** |
| 3 | indexes/ 纯派生缓存，可删不可手改、不进 git | **是** | `test_i3`：`git check-ignore` 对 index.db/rag.db/reading.db 均判真；`git ls-files indexes` 为空；临时库里删光 `indexes/` 后重建，断 index.db 自动回来且检索结果与删前**逐字节相同** | 8 | 是 / 是 | **门禁** |
| 4 | 删除必走软删 → `content/_trash/` | **是** | `test_i4`：走 `/api/delete` 断主文件+2 件旁挂落 `_trash/<YYYYmmdd-HHMMSS>/`、原路径消失、索引不再命中；另**锁死唯一物理删除接口** `/api/inbox/purge` 的越界防护（穿越路径与非 `_inbox` 路径均 400 且文件完好）；再加全仓删除调用点白名单审计 | 12 | 是 / 是 | **门禁** |
| 5 | 分类学权威 = taxonomy.json，代码字典仅兜底 | **是** | `test_i5`：无 JSON → 用内置；写 JSON → 显示名/hue/子域名生效且 `/api/dir/tree` 认到；JSON 未声明的键仍走内置（兜底生效）；JSON 损坏 → 静默回退不抛；断域字典全仓只有 store.py 一处定义 | 8 | 是 / 是 | **门禁** |
| 6 | frontmatter 只存身世，不存阅读统计等运行时数据 | **是** | `test_i6`：跑遍所有会写 fm 的接口后收口断言磁盘 fm 键 ⊆ {title,source,collected,tags,favorite,status}；新文档 stamp 键集精确相等；静态断 `set_fm_scalar/toggle_fm_bool` 的键名参数在白名单内；**阅读事件后语料字节零变化**且统计落 `indexes/reading.db` | 6 | 是 / 是 | **门禁** |
| 7 | 改切块/分词必须递增 `RAG_CODE_VERSION` | **是** | `scripts/check_rag_version.py`（ast 取函数体行区间 + 解析 `git diff -U0` hunk，命中而未递增即 exit 1，纯标准库、缺 git 即 SKIP）+ `test_i7` 的 5 组合成 diff 单测 + 语法错误时判"无法验证"而非静默放行 | 6 | 是 / **部分**（见 §7 局限） | **门禁** |
| 8 | 不依赖 cwd；start.bat / 便携运行时双入口 | **是** | `test_i8`：从项目根之外的陌生目录、以绝对路径 `python <repo>/app/app.py --import-check` + `KB_ROOT` 指向临时目录，断 rc=0、输出 `IMPORT-CHECK OK`、且陌生 cwd 未被写入任何文件 | 3 | 是 / 是 | **门禁** |

**合计 59 条断言**（I1 8 / I2 8 / I3 8 / I4 12 / I5 8 / I6 6 / I7 6 / I8 3）。

**未做成可执行的部分（诚实标注，非"全部覆盖"）**：I5 只断到"代码字典仅一处定义 + JSON 优先"，
**没有**机器手段保证"未来某个新模块不会绕过 `load_taxonomy` 直接读 `DOMAIN_LABELS` 当权威"
（现有 4 处直接引用均已人工核过是 exception 兜底：`learn.py:1105/1111/1117`、`routes_search.py:65/74`、`routes_learn.py:47`、`routes_files.py:436`）。
I4 的白名单审计只覆盖 `app/ scripts/ tests/` 下的 **Python** 侧；JS 侧（`kb-novel.js` 等）没有删除语料的路径，故未纳入。

**I4 专项**（最容易抓真漏洞）：grep 全仓 `os.remove` / `os.unlink` / `shutil.rmtree` / `Path.unlink` / `os.rmdir` / `shutil.move` 调用点，逐个判定是否构成物理删除语料。
**结果：10 个调用点 / 12 次调用，无一作用于正式语料树。** 全部登记为白名单（新增调用点会被 `test_i4` 的审计断言直接拦下，白名单条目腐烂也会报错）。

| 调用点 文件:行 | 作用 | 是否可能删真实语料 | 结论 |
|---|---|---|---|
| `app/routes_files.py:217` `.unlink()` | `api_inbox_purge`：彻底删除 `_inbox/` 下文件 | **否**（作用域双重校验：字符串前缀 + `resolve()` 后重做前缀，非 `_inbox/` 一律 400；且 `_inbox/` 被 .gitignore、不进树不进索引） | 保留。**本轮补 4 条回归断言**（穿越拒绝 / 非 `_inbox` 拒绝 / 正常删除仍可用 / 事后语料树完整） |
| `app/routes_files.py:286` `shutil.move` | `api_rmdir`：整目录树搬进 `_trash` | 否（搬家非删除，目标是回收站） | 保留 |
| `app/store.py:823` `shutil.move` | `rename_domain` / `rename_sub` 迁移目录 | 否（`content/` 内改名搬家，句柄占用兜底） | 保留 |
| `scripts/backup_reading.py:38` `unlink` | 轮转备份只留最近 7 份 | 否（作用于 `backups/reading/`，非语料） | 保留 |
| `scripts/clean_inbox_clones.py:34` `shutil.rmtree` | 清 `_inbox/repos/` 仓库副本 | 否（先验证 `E:/GitHub/<同名>` 原件目录存在才删，作用域限 `_inbox`） | 保留（**一次性脚本，无回归断言**） |
| `scripts/file_inbox_batch1.py:47` `unlink` | 入库后删桌面源文件 | 否（删的是 `content/` 之外的桌面文件，且为一次性迁移脚本） | 保留（**一次性脚本，无回归断言**） |
| `scripts/publish_site.py:107` `unlink` | prune 站仓里已不在白名单的文件 | 否（dest 是站仓；有"缺 `quartz.config.*` 即中止"的守卫） | 保留 |
| `scripts/remap_taxonomy.py:154` `shutil.move` | taxonomy 重映射搬目录 | 否 | 保留 |
| `tests/test_learn.py:666, 677` `unlink` | 临时语料 / 派生库自清理 | 否（`tempfile`） | 断言内 |
| `tests/test_reader.py:241, 355` `unlink` | 临时语料：探针清理 / 模拟外部删除 | 否（`tempfile`） | 断言内 |

---

## 5. 解析入口清单（P3 / P7 靶面，注意分侧）

**Python 侧可导入性实测（原始输出，系统 Python 3.13.7）**

```
app.store    IMPORT-OK   missing=none
app.fts      IMPORT-OK   missing=none
app.sm2      IMPORT-OK   missing=none
app.rag      IMPORT-OK   missing=none
onnxruntime/tokenizers: MISSING -> ModuleNotFoundError No module named 'tokenizers'
```

> 环境事实：本机**无 `.python/` 便携运行时**，系统 Python 缺 `tokenizers`/`onnxruntime` → RAG smoke 按约定自动 SKIP，`/api/rag` 实际不可跑。

**JS 侧导出面实测（原始输出，`static/kb-novel.js`）**

```
$ grep -nE '\bN\.[A-Za-z_]+ *=|window\.KBNOVEL|var N *=' static/kb-novel.js
10:  var N = (window.KBNOVEL = {});
248:  N.savePos = savePos; N.loadPos = loadPos;
347:  N.toolbarHTML = toolbarHTML;
400:  N.decodeTxt = decodeTxt;
（另有 panelHTML/bindPanel/themes/renderTxt/renderEpub/notePrompt/autoStop/registerPaneToc 等）
$ grep -nE 'function (decodeTxt|normalizeEpub|buildChapters|loadPos|savePos)|CHAP_RE *=' static/kb-novel.js
246:  function savePos(rel, obj) {...}
247:  function loadPos(rel) {...}
385:  var CHAP_RE = /^\s*(第[0-9零一二三四五六七八九十百千两]+[章回卷节部集]|Chapter\s+\d+|...)\s*[^\n]{0,40}$/;
387:  function decodeTxt(buf) {...}
402:  function buildChapters(text) {...}
728:  async function normalizeEpub(rawHref) {...}
```

> 结论：手册标注**已核实无误**——`decodeTxt`(400)、`savePos`/`loadPos`(248) 已导出；`CHAP_RE`(385)、`buildChapters`(402)、`normalizeEpub`(728) **未挂到 `KBNOVEL.*`**，浏览器里无法直接调用，需先导出（这本身是一条可提的改进）。

| 解析器 | 侧 | 已导出可测? | 性质测试 | 畸形样本 | 状态 |
|---|---|---|---|---|---|
| `_safe_rel` 路径校验 | Py | 是（但**不在 store.py**，实为 `app.py:163` 闭包 `safe_rel`，经 `routes_edit.py:33` / `routes_files.py:34` 薄包装调用——见 §9 注 3） | 无 | 无 | 未测 |
| frontmatter 解析/写回（`parse_frontmatter` / `dump_frontmatter` / `set_fm_scalar` / `toggle_fm_bool` / `prepend_original_fm`） | Py | 是（store.py:152/173/214/239/251） | 无 | 无 | 未测 |
| taxonomy 改名/迁移（`rename_sub` / `rename_domain`，store.py:732/801，默认 dry-run） | Py | 是 | 无 | 无 | 未测 |
| `fts.py` 双链解析（`extract_wikilinks` / `resolve_wikilink` / `cjk_space` / `cjk_clean`） | Py | 是（fts.py:103/108/139/143） | 无 | 无 | 未测 |
| `rag.py` 切块/分词（`markdown_split` / `_flush` / `_merge_short` / `HFTokenizer`，版本号 `RAG_CODE_VERSION="3"`） | Py | 是（rag.py:214/195/201/64；但真跑需 tokenizers） | 无 | 无 | 未测 |
| `sm2.py` 排程（`schedule` / `q_from_label` / `is_mastered`） | Py | 是（sm2.py:41/82/90） | 无 | 无 | 未测 |
| `KBNOVEL.decodeTxt`（BOM→UTF-8→GB18030→Big5） | JS | 是 | 无 | 无 | 未测 |
| `normalizeEpub` href 解码（OPF/NCX） | JS | **否，需先导出** | 无 | 无 | 未测 |
| `CHAP_RE` / `buildChapters` | JS | **否，需先导出** | 无 | 无 | 未测 |
| `loadPos` / `savePos` 续读 | JS | 是 | 无 | 无 | 未测 |
| `app.js` 正文预处理（`sanitizeFences` 围栏修补、`enhanceArticleDOM` 卡片化） | JS | 否（模块内函数，未挂 window） | 无 | 无 | 未测 |
| `store.is_visible_doc` 可见性谓词（`_` 段 / `.notes.md`） | Py | 是（store.py:275） | 无 | 无 | 未测 |
| `routes_pages._rewrite_html_assets` 美化版依赖重写 | Py | 否（模块私有） | 无 | 无 | 未测 |

**§5 汇总：13 条解析入口。**

---

## 6. 发现台账（所有轮次累积，只增不删）

| # | 日期 | 严重度 | 现象 | 复现步骤 | 涉及文件:行 | 根因 | 修复 commit | 回归断言位置 |
|---|---|---|---|---|---|---|---|---|
| 1 | 2026-09-23 | 功能错误 | **CI 覆盖面小于本地 pre-commit**（门禁落差）：`.github/workflows/ci.yml` 只跑 `test_reader` / `test_new_project` / `test_rag` 三套 smoke，且**从不跑 ruff**；而 pre-commit 跑的是 ruff + 4 套 smoke（含 learn/govern）。结果是"本地拦住、CI 放行"——PR 走 GitHub 流程时，ruff 回归网与 learn/govern 三套断言形同虚设 | `cat .github/workflows/ci.yml` 对比 `.githooks/pre-commit`；数 smoke 调用行数与 ruff 行 | `.github/workflows/ci.yml:1-31`；`.githooks/pre-commit:37-68` | CI 是后加的，未随 pre-commit 链演进同步 | **P2 已修**（ci.yml 补齐 ruff + learn + govern + invariants） | 门禁本身（CI 步骤），非断言 |
| 2 | 2026-09-23 | 数据丢失（潜在） | **`/api/inbox/purge` 的越界防护无任何回归断言**：它是全仓**唯一**物理删除正式路径可达文件的接口（`app/routes_files.py:217` 的 `t.unlink()`），而代码注释自己记载着"穿越串能过字符串前缀却落在正式树里（临时夹具实测逮到，误删 1 篇后修复）"。修复后只改了代码，**没留下断言**——`grep purge tests/` 命中 0。任何人重构那段前缀校验都可能把误删重新引入且无人察觉 | `grep -rn purge tests/` → 0 命中；读 `app/routes_files.py:195-221` | `app/routes_files.py:203-210`（校验）/ `tests/`（缺断言） | 热修时只补代码未补回归，与 AGENTS"未跑测试不得宣称完成"的取向相悖 | **P2 已补断言**（`tests/test_invariants.py::test_i4` 4 条：穿越拒绝 / 非 `_inbox` 拒绝 / 正常删除仍可用 / 事后语料树完整） | `tests/test_invariants.py` I4 组 |
| — | — | — | P0 为纯枚举阶段，未产生经实证的缺陷 | — | — | — | — | — |

> 补充：P1 关闭的两条**已确认缺口**（由手册预先指出，非本轮新发现，故不重复占用发现编号）：
> ① `package.json` 的 `lint:js` 漏 `static/kb-novel.js`（实测 996 行、51305 字节的小说引擎整份从未被 eslint 扫过，独跑报 21 条 warning）—— 已修，见 §7；
> ② `.githooks/pre-commit` 不跑 eslint/stylelint —— 已接入，见 §7。

### 已划掉/合并的格子（必须写理由，否则视为掩盖盲区）

| 格子 | 日期 | 理由 |
|---|---|---|
| §2「搜索浮层」独立行 | 2026-09-23 | 引擎 chips ×4 / 范围 chips ×11 / #kb-so-q / #kb-so-clear 已按实际 DOM 归属计入「正文区」就地搜索浮层行，避免同一控件两处计数（虚增格子）。填「不适用+去重」保留可见性。 |

---

## 7. 静态门禁现状（P1 建、P2 补齐 · 2026-09-23）

| 门禁 | 覆盖范围 | 进 pre-commit? | 进 CI? | 缺口 |
|---|---|---|---|---|
| ruff | 全 Py（排除 indexes/content/.python 等） | 是 | **是（P2 补齐）** | — |
| eslint `npm run lint:js` | app.js / kb-core.js / **kb-novel.js（P1 补入）** / motion.js / pages/*.js | **是（P1 接入）** | 否（CI 无 setup-node/npm ci） | **现存 115 warnings / 0 errors**（补入前 94）；warnings 不致失败，故基线可观 |
| stylelint `npm run lint:css` | static/**/*.css | **是（P1 接入）** | 否 | 本轮实测 **0 告警**（干净） |
| `check_dangling_tokens.py` | CSS 令牌 | 是 | 是（P2 补齐） | 仅令牌，未覆盖 URL 断链 |
| `scripts/check_rag_version.py`（I7） | `app/rag.py` 切块/分词函数体的 diff | **是（P2 接入）** | 否（见下方局限） | 依赖"工作区/暂存区 vs HEAD"的 diff |
| `tests/test_invariants.py`（I1~I8） | AGENTS 不变量 8 条 | **是（P2 接入）** | **是（P2 接入）** | 59 条断言；I5 的"代码字典仅兜底"只到人工核过引用点，无机器强制 |
| bandit | — | 否 | 否 | **未安装（待决策）** |
| semgrep | — | 否 | 否 | **未安装（待决策）** |
| 6 套 smoke | `tests/`（reader / new_project / learn / govern / rag / invariants） | 是 | **是（P2 补齐 learn + govern）** | 断言有效性未经变异测试度量（P6） |

**bandit / semgrep「未安装（待决策）」的代价（P1 明确延后，本轮不装不接）**：

- 两者都是 **pip 新依赖**（bandit 纯 Python、semgrep 在 Windows 需 `pip install semgrep` 且体积不小），违反本轮"零新依赖"约束。
- 接入 pre-commit 后，**每个开发机都必须装**才能跑（不像 eslint 有"没装就 SKIP"的天然降级）；且 semgrep 规则包若要离线需预缓存 `--config p/default`（默认会联网拉规则），与本仓库"语料不出网"的取向冲突。
- 真收益点（手册 §P1 已列）：`except Exception` 静默吞异常（F821 被吞成错数的教训）、`subprocess`/`shell=True`、路径拼接。**其中"路径穿越"已由 `tests/test_reader.py` 的 `/raw` 穿越拒绝用例 + P2 的 I4 删除路径审计覆盖**，故边际收益低于成本。
- 决策建议（留待用户定）：若要做，**只接 bandit 不接 semgrep**（纯 Python、无网络、Windows 无障碍），且只扫 `app/ scripts/`。

**I7 静态检查的已知局限（写清楚，别当它万能）**：判定基准是"暂存区/工作区 vs `HEAD`"的 diff。
在**本地 pre-commit** 场景它是准的（正要提交的内容就在暂存区）；但在 **CI 的全新 checkout** 里没有工作区 diff，
该检查会退化为空转（每次都 ok）。要让 PR 场景也生效，需把比较基准换成 base 分支（`origin/${{ github.base_ref }}`），
属下一步可做项——本轮**没有**接进 CI，就是为了不制造"看起来在跑其实没跑"的假门禁。

**P1 + P2 实测原始输出（门禁真跑的证据）**

```
$ npm run lint:js        （补入前）✖ 94 problems (0 errors, 94 warnings)  ← 覆盖 9 个文件，不含 kb-novel.js
$ npm run lint:js        （补入后）✖ 115 problems (0 errors, 115 warnings) ← 覆盖 10 个文件，含 kb-novel.js
$ npx eslint static/kb-novel.js  → ✖ 21 problems (0 errors, 21 warnings)  ← 独跑该文件的问题数
$ npm run lint:css       → 无输出，EXIT=0（干净）
$ python tests/test_invariants.py → 59 passed, 0 failed, 0 skipped（EXIT=0）
$ sh .githooks/pre-commit → 见下（EXIT=0，含新接入的 eslint / stylelint / invariants / rag-version 四步）
```

```
[pre-commit] using interpreter: python
[pre-commit] running ruff (regression net)...
[pre-commit] running reader smoke...
82 passed, 0 failed
[pre-commit] running scaffolder smoke...
9 passed, 0 failed
[pre-commit] running learn/review smoke...
225 passed, 0 failed
[pre-commit] running govern/stats smoke...
GOVERN+STATS TESTS OK
[pre-commit] running eslint (npm run lint:js)...
✖ 115 problems (0 errors, 115 warnings)
[pre-commit] running stylelint (npm run lint:css)...
[pre-commit] running invariants gate...
59 passed, 0 failed, 0 skipped
[pre-commit] running dangling-token check...
dangling tokens: 0 (all var() uses resolve to definitions / fallback / runtime-injected)
[pre-commit] running RAG_CODE_VERSION check...
[rag-version] ok: app/rag.py 本次无改动
[pre-commit] running RAG smoke (SKIP if deps missing)...
RAG TESTS OK
[pre-commit] all green.
```

**I7 门禁的"故意破坏 → 拒绝 → 复原"实证（原始输出）**

```
# ① 只改 markdown_split 函数体（加一行），版本号不动 → 脚本拒绝
$ python scripts/check_rag_version.py
[rag-version] 拒绝提交：切块/分词逻辑改动，但 RAG_CODE_VERSION 未变。
[rag-version]   命中函数：markdown_split
[rag-version]   请递增 app/rag.py 顶部的 RAG_CODE_VERSION，它会触发向量索引全量重建（不变量 7）。
exit=1

# ② 同一改动 + 把 RAG_CODE_VERSION 从 "3" 改成 "3-TEMP" → 放行
$ python scripts/check_rag_version.py
[rag-version] ok: 命中 markdown_split 且已递增 RAG_CODE_VERSION
exit=0

# ③ 整条钩子在①状态下被拦（其余 8 步全绿后才卡在最后一步）
$ sh .githooks/pre-commit
[pre-commit] ... ruff/4 套 smoke/eslint/stylelint/invariants/dangling 全部通过 ...
[pre-commit] running RAG_CODE_VERSION check...
[rag-version] 拒绝提交：切块/分词逻辑改动，但 RAG_CODE_VERSION 未变。
[rag-version]   命中函数：markdown_split
HOOK_EXIT=1

# ④ 复原后 sha256 与改前逐字节一致、git status 干净
$ sha256sum app/rag.py → 01ed6d0db866856f1a063c9f8ee95e018e972a61bd773703bf8235a02ce8b3cc
$ git status --short -- app/rag.py → （空）

# ⑤ SKIP 路径：git 读不到 HEAD 时放行而非挂掉
$ GIT_DIR=/nonexistent/.git python scripts/check_rag_version.py
[rag-version] SKIP: 读不到 HEAD:app/rag.py（首次提交或非 git 仓库）
exit=0
```

---

## 8. 下一步（每轮收工填，只写一个动作）

- [x] ~~执行 **P1 · 静态层补漏**~~（2026-09-23 完成，见 §7）
- [x] ~~执行 **P2 · 不变量门禁**~~（2026-09-23 完成，见 §4 / §7）
- [ ] 执行 **P4 · 端到端冒烟**（零新依赖，复用 `scripts/agent/{evalcdp,shot}.mjs`）：起 5031 临时实例，
      遍历 §1 全部 58 个端点 + §2 的 110 个控件，把"无 CONSOLE_ERRORS"变成断言，收编 `scripts/api_patrol.py`（现打用户常驻 5001，改写为打 5031 临时实例）。
      **理由**：本轮把"静态能不能进主干"补齐了，但 §1/§2/§3 三张表仍全 `未测`（166 → 158 格未覆盖，占比仍最高），而手册把 P4 列在性价比第 3 位。

> 顺带（不单独占轮次）：I7 检查若要覆盖 PR 场景，需把比较基准从 `HEAD` 换成 base 分支（见 §7「I7 局限」）；bandit/semgrep 的取舍待用户决策（见 §7 代价表）。

---

## 9. P0 口径偏差与存疑（本轮新增，供后续轮次核对）

> 记录的是**手册/台账描述与代码事实不符**之处，不是产品缺陷。列在此处避免下一轮又按错前提去查。

1. **`create_app()` 有副作用**：`app.py:102` 会 `index_is_stale()` → 可能 `build_index()` 写 `indexes/`；且 `app.py:268` 模块级 `app = create_app()` 一 import 就跑。为守"只读"约束，本轮所有枚举命令均把 `KB_ROOT` 指向 `tempfile.mkdtemp()` 的一次性目录并在同进程 `shutil.rmtree` 清理，**仓库 `indexes/` 与 `content/` 零写入**。后续轮次若直接照手册命令跑，会真实重建本机索引——功能上无害（派生缓存），但请知悉。
2. **手册 P0 第 2 条写"右栏五页签"，代码实为 4 个**（目录/标签/备注/双链，`workbench.html:77-80`）。多出来的那个不存在，别去找。
3. **手册 P0 第 4 条把 `_safe_rel` 归到 `store.py`，实际不在**：单一实现在 `app.py:163` 的闭包 `safe_rel`，`routes_edit.py:33` 与 `routes_files.py:34` 只是 `_hooks()["safe_rel"]` 的薄包装（同一实现三处调用点，**无逻辑重复**，已逐行比对）。P3 做性质测试时应直接测 `app.py` 的那个闭包或经 HTTP 打。
4. **环境与手册不符**：手册"仓库现状事实"表写 Node v24.19.0、本机实测 `v24.18.0`；手册称有 `.python\` 便携运行时，本机**不存在 `.python/`**，系统 Python 3.13.7 缺 `tokenizers`/`onnxruntime`。
5. **`scripts/api_patrol.py` 是现成的活体巡检器**（11 个写接口，写临时文档、测完软删），但它打的是 `http://127.0.0.1:5001`（用户常驻实例），**不是自动化门禁**。P4 可考虑把它改成起 5031 临时实例后跑，直接收编成端点 E2E 的一半。
6. **§9 注 4 的"缺 tokenizers/onnxruntime"已被 P2 实测修正，措辞要收窄**：本机系统 Python 3.13.7 **确实缺 `tokenizers`**（`import tokenizers` → ModuleNotFoundError，P2 实测），
   但 **`onnxruntime` 可导入**——所以 `tests/test_rag.py` 走的是**真实执行**而非 SKIP（实测输出 `ok markdown_split` / `ok tokenizer` / `ok end-to-end` + `RAG TESTS OK`），
   `tests/test_invariants.py::test_i2` 的 RAG 语料集断言也因此真跑（`0 skipped`）。
   依据：实测 `onnxruntime` **1.30.0 可导入**、`sqlite_vec` **可导入**，只有 `tokenizers` 缺（`test_rag.py:20` 只用 `onnxruntime` 作依赖探针；
   `tokenizers` 在 `HFTokenizer.__init__` 内懒加载且不影响 end-to-end —— 实测"语义检索命中正确文档，score 极性正确"真跑通）。
   **结论修正**：本机"RAG 全套跑不了"的说法不准确，应表述为"缺 `tokenizers`，分词走回退路径，但 RAG smoke 仍真实执行"。
7. **`content/_inbox/` 与 `content/_trash/` 都被 .gitignore**（`.gitignore:2` / `:12`，`git ls-files content/_inbox` = 0）。
   这条支撑了 I4 的判定口径：`/api/inbox/purge` 的物理删除不构成"不可再生语料丢失"——`_inbox` 是迁移暂存区，
   正式树文档一律走 `_trash` 软删（AGENTS 不变量 4 的语义完整保留）。**改 .gitignore 会推翻这条判定**，届时 I4 的白名单结论需重审。
