# BACKLOG — 待办与延后项

> 约定：这里只记"已决定要做但还没做"的事。做完就划掉；新想法先进这里，不中途插队改正在做的东西。

## 待办

- [ ] **阅读统计增强（统计 v1 已上线后按需）**：finish 事件自动判定（字数/400 阈值已定义，
  前端未接）；断点续读；月度对比视图。
- [ ] **每日自动备份计划任务**（2026-09-08 用户指示押后）：脚本已就绪且 2026-09-09 升级 v2
  （根路径自动推导，任意克隆可用；注册命令见 `scripts/daily_backup.ps1` 头注释）；
  等用户在现场监督时再注册 schtasks。
- [ ] **Git 提交规范沉淀**（2026-09-09 从 HANDOFF 移入）：用户 2026-09-08 全局指令
  "每开发一部分就自动 commit、信息写详细、用户统一手动 pull"——已在执行（95ec573 起遵循），
  待用户批准后写入 AGENTS.md 常驻规则。

## 已延后（按用户决定排序）

- [ ] **AI金（ai-jin）108 份文档导数据**——用户正在自行重新整理（重复、过时内容多），放最后。背景：时间跨度 8 年、存储散布云端 / GitHub / 本地 / 台式机 / 笔记本 / 公司电脑。整理完成后：`new_project.py AI金` 建格子 → 迁入 docs/ → 合并桌面已收编的 `projects/不锈钢市场/项目说明-AGENTS.md`。
- [ ] 桌面项目区（code / dsh / feishu_code / pg-tools / 素材）逐目录归位——须先经用户逐目录确认，不批量下结论。
- [ ] work 仓库 108 份中非 ai-jin 的零星文件（如有）。
- [ ] 静态发布管线（VitePress / Quartz 导出，公开分享用）。
- [ ] **GitHub 仓库可见性确认**（2026-09-09 审读新增，用户指示放最后）：语料含简历/面试/复盘等
  敏感材料，需确认远端为 Private；若为 Public 则与 README"语料不出网"承诺冲突。
- [ ] **_inbox 冷备份**（2026-09-09 审读新增，用户指示放最后）：391 份暂存 md
  （desktop 280 + repos 108 + 3 份清单）被 .gitignore 排除，git 与每日备份均不覆盖，
  需压缩/镜像到另一块盘。
- [ ] **本机开发护栏启用**（2026-09-09 审读新增，用户指示放最后）：
  `git config core.hooksPath .githooks`（恢复 pre-commit 测试护栏）；
  如需语义检索再补装 `requirements-rag.txt` 并首跑拉取模型。

## 已完成

- [x] **右键菜单三期**（2026-09-08，commit 6e24a91）：`/api/move/batch` 批量归档
  （逐条级联索引、部分失败不回滚、per-item 报错）、`/tags` 多选批量合并（两段确认）、
  `/api/substats` 目录级统计（workbench 子目录右键弹窗：篇数/字数/标签分布/最近更新）。
- [x] **标签合并执行**（2026-09-08，commit ae97679）：「AI」→「AI资产」9 篇，
  census 复核独立 AI 标签清零、AI资产 10 篇。
- [x] **文档管理右键菜单一期**（2026-09-08）：文档右键 移动/重命名（级联 FTS+双链+向量索引）、
  复制 [[双链]]、复制 Obsidian URI、统计信息（/api/stats）；目录右键 新建文档、复制路径。
  设计约束三条全部落实（级联更新 / 统计走独立库 / 保留软删除）。
- [x] **向量 / RAG 检索（2026-09-07 上线）**：本地管线（无 API 依赖）——bge-small-zh-v1.5
  ONNX 嵌入 + sqlite-vec + 手写 WordPiece 分词（与 tokenizers 库逐 token 对齐验证）。
  14791 块索引 309s 重建、查询 ~60ms；入口：`?` 语义搜索 / `/api/rag` / scripts/rag_search.py。
  过程产物：`.python\` 便携运行时（git 忽略）、tests/test_rag.py。
- [x] **标签管理页（2026-09-07）**：`/tags` 路由，按文档数降序展示所有标签，点击展开查看关联文档。
- [x] 递归扫描 / 扫描缓存 / 软删除（2026-09-06）
- [x] 收件箱克隆副本清理 3073 份（2026-09-06）
- [x] 项目骨架脚手架 new_project.py（2026-09-06）
- [x] 阅读器增强：mermaid 渲染、代码高亮、TOC 滚动高亮（2026-09-06）
- [x] ~~阅读器图谱视图~~ 2026-09-06 上线后同日按用户要求移除（观感装B无实用），代码在 git 历史 207bc13 可随时恢复；双链解析与反链面板保留。
- [x] **切页卡顿根治（2026-09-06）**：分类树进 localStorage + 客户端路由（点目录/文档只 fetch 正文，整页不再刷新），实测切文档 ~20ms。
- [x] **反链面板 + 双链解析（2026-09-06 上线）**：右侧"双链"标签，"谁引用了它/它引用谁"，未解析链接标警告（整理时的断链清单）；建索引时解析并落库，代码块内假双链已剔除。
- [x] **即存即搜（2026-09-07）**：后台线程每 30 秒检测 mtime 变化，新文件/修改自动入索引，无需重启。
- [x] ~~**清理已合并的旧仓库（2026-09-07 用户确认完成）**~~ 原条目：本地 `E:\GitHub` 各仓库 + GitHub 远端仓库，凡是内容已收编进知识库的一律清理。执行清单：① 对照 INVENTORY / scan 记录核对内容已全部收编、无未迁移文件；② GitHub 远端仓库用 **Archive** 标记归档（可逆），不直接 Delete；③ 本地目录先移动到冷备份盘，观察一段时间后再真删；④ 涉及公司资产的仓库单独列清单，逐仓经用户确认后再动。
